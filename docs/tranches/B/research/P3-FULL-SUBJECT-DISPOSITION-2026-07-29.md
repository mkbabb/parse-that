# P3 full-subject disposition

Date: 2026-07-29

Pass: `P3`

Disposition: **ROW-COMPLETE ADVERSARIAL PASS; ZERO CANDIDATES ADMITTED; FORMATION RED**

P3 re-runs the complete twenty-row P1/P2 subject against the smallest
remaining executable hypothesis. It binds two new coordinates:

- same-FIRST recovery at `f4b9abb`, manifest-file SHA-256
  `90115ccd7b567591c56e68870605331f99078be4baf246a10e246649829ae49e`;
- direct closure at `19c1e12`, artifact directory
  `artifacts/pass3/p3-direct-closure/`.

Accepted M2 `de36d57dccdd20068b8c11a78f6e83d42e7d681f` remains the control.
P1 is `69f72f7`; P2 is `c3d42d4`. This document grants no isolated proof,
formation, candidate pack, consumer, execution, release, Value CSS or BBNF
credit.

## Row-complete adjudication

| Row | Full subject | P3 executable finding | Disposition |
|---|---|---|---|
| `P3-01` | Ownership and release order | Both P3 transactions are private and generic. They add no production source, export, CSS/domain type, consumer edge or package. | `GREEN LAW` |
| `P3-02` | Archaeology and prior claims | The final surviving hypothesis is measured in one process over eleven warmed alternating AB/BA batches at six fixed-grammar scale points, matched success state/value/result views, two projected failure-state subsets, memory, CPU and mixed V8 traces. There is no bootstrap, build-plus-first/cold parse, alternating grammar-shape plane, complete state surface or failure-result projection. The 1.0656–1.5238× range is nevertheless fatal to the candidate. | `BOUNDED EVIDENCE; CANDIDATE KILL` |
| `P3-03` | Scalar transaction and rollback | The direct kernel uses only offset/value/diagnostic-length/error rollback with sticky faults. Same-FIRST evidence proves accepted-M2 traversal provenance without another checkpoint field. | `KEEP LAW` |
| `P3-04` | Run-scoped state and isolation | Every parse constructs one run state; diagnostics, frontier, nesting and values are run-owned. There is no parser-retained result, memo epoch or mutable source-global data. | `KEEP LAW` |
| `P3-05` | Exact sequence slots | The six fixed-grammar success products preserve the values and authored tuple/array positions visible through the bounded state/value/result views. Top-level suggestions, secondary spans and recursion counters are not projected. | `BOUNDED KEEP; COMPLETE PLANE RED` |
| `P3-06` | UTF-16 positions and spans | The projected JSON and fixture values preserve their authored `{start,end}` spans over the frozen inputs, including CRLF, astral JSON content, escape text, URL, nested-call and EOF products. No decoded position plane exists; no claim is made for omitted top-level state evidence. | `BOUNDED KEEP` |
| `P3-07` | Diagnostics and recovery | One direct-closure recovered success and two diagnostics-on failure-state subsets are exercised. The direct same-FIRST test checks success, final offset and the six furthest-offset values only; exact slots, labels, spans and diagnostic bytes remain supplied solely by the separate accepted-M2-versus-D bank at `f4b9abb`, not by this candidate. No scaled diagnostics-on recovered-success plane exists. | `KEEP LAW; CANDIDATE EQUIVALENCE RED; D KILL CONFIRMED` |
| `P3-08` | Result materialization | Consumer-owned success projection is equal through the bounded view at all six scales and remains outside the parser surface. Its ratios are only 1.0762–1.4561×. Failure value/result projection is absent. | `BOUNDED KEEP; PERFORMANCE/EQUIVALENCE RED` |
| `P3-09` | Literal terminal dispatch | The candidate uses direct literal closures and native sticky RegExp only. No graph/table or second terminal API returns. Measured warmed success products top out at 1.5238×. | `KEEP INCUMBENT; NO CANDIDATE` |
| `P3-10` | Cold/hot dispatch | Separately warmed grammar-construction calls are 1.9534× faster for JSON and 1.7118× for the fixture, but no build-plus-first/cold parse or alternating-shape plane exists and every warmed product misses 10×. No bootstrap is permitted. | `RED / CLOSED` |
| `P3-11` | Recursion and nesting | Cached lazy recursion, live/max depth and typed pre-`RangeError` nesting are green over the candidate-only frozen JSON corpus and hostile depth case. Measured warmed JSON success ratios are 1.0656–1.4623×. | `BOUNDED KEEP; PERFORMANCE RED` |
| `P3-12` | Memo policy | The candidate has no packrat dependency. No second live consumer emerged, so opt-in packrat remains excluded from a successor surface. | `PRUNE ROUTE` |
| `P3-13` | Unordered `&&` / `\|\|` | No unordered engine is present. The same-FIRST fixture uses a private consumer-owned exhaustive search only to bind traversal semantics; it does not revive S or D. | `S/D KILL CONFIRMED` |
| `P3-14` | Generic CSS-needed leaves | The candidate uses the incumbent native sticky RegExp and direct literal closures. Equal warmed success projections reach only 1.0656–1.5238×, so no new leaf abstraction is admitted. | `KEEP INCUMBENT; RED` |
| `P3-15` | Allocation and retained heap | The 510-line kernel cuts 66.9% versus the broader 1,543-line M2 file set and stable grammar heap by 40.5%, but retained 96-statement parse-state heap grows 12.4%. | `STRUCTURAL HOLD; CANDIDATE KILL` |
| `P3-16` | IC, hidden classes, deopt and GC | CPU exposes no order-of-magnitude seam. The mixed trace has 168 GC events, 88 bailouts and 108 dependent-code invalidations. Stable mutable arrays and source-loop line counting were measured and rejected. | `RED / NO CLEAN SEAL` |
| `P3-17` | Isolated Value-shaped product | Eighteen warmed fixture state/value/result projections at 4/8/16/33/96/753 are equal through the bounded view, including value-embedded spans and diagnostics-off opaque recovery. No scaled diagnostics-on recovery, complete state, cold or alternating-shape proof exists. Ratios are 1.1249–1.5238×. | `BOUNDED CORRECTNESS; PERFORMANCE/OMITTED PLANES RED` |
| `P3-18` | Isolated JSON-shaped product | Candidate-only tests match `JSON.parse` for 33 valid inputs and reject 7 invalid inputs with nonempty authored frontiers; invalid-state equality to M2 is not asserted. Eighteen warmed scaled JSON success projections are equal to a synthetic M2-primitive `any()` grammar, not the live dispatch-based M2 `jsonParser`. Ratios are 1.0656–1.4623×. | `BOUNDED CORRECTNESS; CONTROL/OMITTED PLANES RED` |
| `P3-19` | Public surface and pruning | P3 adds zero exports. Its private kernel demonstrates that compiler/table/unordered/source-leaf/result/packrat/`/utils` surfaces are unnecessary for these products, but a killed prototype cannot define a successor. | `PRUNE ROUTE; NO SUCCESSOR` |
| `P3-20` | Full CSS, WPT/browser and UI | No CSS grammar or UI was authored. Value remains sole owner after formation and immutable packing; current Value formation supplies zero parser execution credit. | `ROUTED / ZERO CREDIT` |

## Ruling

P3 is row-complete. The smallest direct, scannerless, idiomatic combinator
surface is materially smaller than the broader incumbent and is
correctness-green only for the bounded success projections and hostile tests
named above. Complete-state, failure-result, invalid-M2, cold/first,
alternating-shape, bootstrap and scaled diagnostics-on recovery planes remain
RED. Those omissions cannot rescue a point range already more than sixfold
below the binding 10× floor at its best measured success point, nor its larger
retained parse products. The direct kernel is therefore terminally killed as
a runtime candidate without promoting the bounded evidence to isolated
every-subject proof.

P1, P2 and P3 are complete adjudication records with zero surviving
candidate. The next formation edge is not Clean A, packing or Value
execution. The isolated every-subject ≥10× prototype remains RED because no
candidate exists. A future formation transaction must introduce a genuinely
new generic mechanism that preserves every P3 law and clears every point
estimate before bootstrap; renaming or recombining a killed compiler, table,
VM, scanner, source leaf, unordered engine or private closure runtime earns
no new pass.
