# P7 — exact structural authority and budget RED

Status: `PAPER_V7_RED / P7_EXECUTABLE_AST_BUDGET_UNBOUND / ZERO_CREDIT`.

The sole registry materializes `C30–C43` as fourteen distinct canonical source,
graph, AST, budget, tool, and claim fixtures/domains. Exact member bytes/hashes,
locators, replays, masks, complements, collateral, and all fourteen full P7
leaf identities are machine-bound. The final 182 `nonownerRetentions` entries
enumerate every P7 control/nonowner pair; machine arithmetic remains 240.

P7 still has no source parser, AST tool, adapter, verifier, harness, observer,
transitive graph, or measured charged LOC. `p7ActualChargedLoc` is therefore
absent/RED; no work may be excluded as generated, external, or test-only.

P7 participates only in `E12–E18`. Hidden evaluators, dynamic code/reflection,
hidden dependencies, scanner/token/index/tape/CST/forest, event projection,
opcode VM, fallback, dual path, CSS grammar in parse-that, and parser→Fourier
authority remain forbidden.
