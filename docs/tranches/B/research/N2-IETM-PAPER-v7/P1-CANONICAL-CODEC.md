# P1 — exact canonical codec domains

Status: `PAPER_V7_RED / ZERO_CREDIT / SOURCE_WITHHELD`.

PAPER-READY materializes ten distinct raw fixtures `FX-C20–FX-C29`, their
closed schemas, baseline/audit member bytes and hashes, byte locators, replay
masks, complements, and collateral. No incompatible raw case shares a fixture.
Duplicate keys, alternate escapes/order/integer/UTF-8/envelope/tag forms are
therefore byte-reconstructable rather than labels.

The sole machine schemas also close `RawProductionInput`, `ValueDomain`, audit
admission, signature, and observation membership. P1 participates only in
`E01/E07–E11/E13`; no second codec, reparse, scanner/token plane, CSS grammar,
or fallback exists.
