# P6 — exact production-control authority

Status: `PAPER_V7_RED / ZERO_CREDIT / SOURCE_WITHHELD`.

The sole registry materializes `C01–C29` as 29 distinct schemas, fixtures, and
domains. Each row points to exact baseline/audit members, locator, byte replay,
measured mask/complement, collateral, injector, full leaf identity, audit-only
wrapper, same-body suppression, and exact byte-no-op. Production has no
suppression or expected-code argument.

The first 812 entries in `nonownerRetentions` enumerate every P6
`control × nonowner leaf` pair. Row ranges point into that array; no selector,
wildcard, or `OTHER*` label remains. Machine arithmetic remains 930 receipts.

P6 participates only in `E07/E08/E18–E23`. Detached raw bytes, reviewer reissue,
shadow predicates, CSS grammar, and Fourier authority are RED.
