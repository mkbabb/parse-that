# P0 — authenticated ledger and selection

Status: `PAPER_V7_RED / ZERO_CREDIT / SOURCE_WITHHELD`.

PAPER-READY is the sole machine authority. P0 consumes the exact schemas,
fixture bytes, member hashes, locators, replay masks, and leaf identities for
`C01–C05`; none is supplied by prose. Declaration, ledger, version parent,
effect key, and experiment selection each have a distinct closed fixture and
domain. The typed selection is ledger-derived and cannot be a caller label.

P0 participates only in machine edges `E01/E02/E04/E12/E19/E26`. It owns no
executor, future output, suppression input, CSS grammar, or Fourier edge.
