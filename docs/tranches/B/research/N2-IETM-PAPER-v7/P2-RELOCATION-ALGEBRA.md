# P2 — relocation algebra

Status: `PAPER_V7_RED / ZERO_CREDIT / SOURCE_WITHHELD`.

`SCHEMA-C06-V1`, `FX-C06`, and `DM-C06` bind the exact edit fixture, member
hashes, unique locator, replacement bytes, measured mask/complement, replay,
and `validateEditReplay` identity. P2 retains point/interval/EOF/depth and
epoch-aware composition laws, while P3 alone validates relocated products.

P2 participates only in `E02/E03/E14/E20`; there is no caller relocation map,
public edit API, CSS grammar, or Fourier edge.
