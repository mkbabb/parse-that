# P0 — signed declaration and dispatch authority

Status: **PAPER RED / zero credit**.

P0 owns the externally pinned V10 trust key, ledger root, signed registry message, declaration/version/selection/effect invariants, and the exact dispatch authority. The signed message roots schemas, fixtures, production and alternate instances, expectations, all proposed source bytes, derived leaf spans, controls, audit families, traversal rows, and all nonowner rows.
