# P1 — strict raw invariants

Status: **PAPER RED / zero credit**.

C20–C29 retain exact canonical bytes and two independent invalid values apiece. The same authenticated validator dispatch owns UTF-8, duplicate-key depth, escaped slash, hex/control spelling, key order, integer form, whitespace, and tag closure. No source was executed.
