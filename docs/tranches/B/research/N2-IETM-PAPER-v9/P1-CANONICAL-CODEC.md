# P1 — strict canonical raw authority

Status: **PAPER RED / zero credit**.

P1 owns C20–C29. Exact raw bytes are the consumed production field. The proposed validator contains its strict UTF-8 and canonical JSON decoder: duplicate-key depth, escaped slash, hex/control spelling, key order, integer form, scalar validity, whitespace, and closed tag membership are derived from bytes. Each control carries two distinct invalid byte strings and one exact canonical baseline. No source was imported or executed.
