# P7 — source architecture and verifier boundary

Status: **PAPER RED / zero credit**.

P7 owns C30–C43 over the exact proposed validator, injector, and audit-wrapper bytes. Module set, hashes, prose, call edges, forbidden reverse edges, dynamic import/code/reflection, declared imports, forbidden substrates, LOC budgets, AST-tool pin, and claim/source join are derived from authenticated bytes. The three modules were materialized but never imported, parsed, checked, or executed. No CSS grammar, fallback, dual path, or Fourier edge is present.
