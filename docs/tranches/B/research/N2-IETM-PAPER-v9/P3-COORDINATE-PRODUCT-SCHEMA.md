# P3 — coordinate-bearing product authority

Status: **PAPER RED / zero credit**.

P3 owns C07–C08. Depth is derived as entryDepth plus maxDepthDelta; offset must equal terminalOffset and bound every span. BEFORE, AFTER, and two alternate-invalid objects share an exact closed schema. Product execution and equivalence remain withheld.
