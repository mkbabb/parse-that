# P6 — production leaves and audit wrapper

Status: **PAPER RED / zero credit**.

P6 binds 43 exact leaf identities to materialized validator bytes. The materialized injector applies one authenticated pointer operation and recomputes the entire production root. Production validation exposes no suppression argument. The audit-only wrapper retains the same mutant for owner suppression and control-of-control; all 1,806 ordered nonowner rows bind mutant root, owner leaf root, disabled nonowner leaf root, and expected owner code. Unknown, duplicate, and broad refusal are source-owned. All results are planned, not observed.
