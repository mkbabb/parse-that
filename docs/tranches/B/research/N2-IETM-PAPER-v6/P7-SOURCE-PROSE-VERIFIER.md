# P7 — structural controls and executable-budget RED

Status: `PAPER_V6_RED / P7_EXECUTABLE_AST_BUDGET_UNBOUND / ZERO_CREDIT`.
Future ceiling: `1 module / 110 charged LOC`.

PAPER-READY is the sole machine table. P7 references ordered IDs `C30–C43`
only; it does not repeat row content. The registry derives fourteen exact P7
`LeafFunctionIdentity` records and `240` future receipts.

P7 owns source/prose/interface pins, source parser, AST representation,
adapter, verifier, harness, observer, audit wrapper, dependency graph, metric
counter, and structural receipts. All executable/transitive bytes count inside
P7, including external/tool/generated/test/harness/owner-side work.

The source-hash control retains the production owner pin. Every other
structural mutant uses a separately owner-admitted audit envelope whose source
rebase and exact changed mask are dictated by its machine row, so source hash
cannot preempt the intended owner. The audit admission never substitutes a
production admission.

Future ceilings remain `120/155/90/150/70/85/70/110 = 850`. No P7 source
parser, AST tool, adapter, verifier, harness, observer, transitive graph, or
actual charged LOC exists. `p7ActualChargedLoc` remains `null`; excluding that
work or recording zero is RED.

P7 participates in `E12–E18`. Closed bans include hidden evaluator, reparse,
dynamic code/reflection, hidden dependency, scanner/token/index/tape/CST/
forest, event-region projection, opcode/VM, fallback, dual path, CSS grammar in
parse-that, and parser→Fourier authority. No source or budget credit follows.
