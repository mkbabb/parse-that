# P3 — coordinate-complete actual products

Status: `PAPER_V6_RED / ZERO_CREDIT / SOURCE_WITHHELD`.
Future ceiling: `1 module / 150 charged LOC`.

P3 retains the closed typed memo entry, complete product, dependency-read,
terminal/fault/recovery/diagnostic/selection/span, prototype/descriptor/symbol,
hole/cycle/alias, and immutability schemas. `CoordinateValue` remains legal in
all semantic graph positions. One visitor relocates every coordinate exactly
once per graph identity while preserving aliases, cycles, descriptors,
prototypes, symbols, holes, ordering, and frozen state.

Candidate/control products arrive only through admitted P5 post-run seals
under the immutable precommitted ExperimentPlan. P3 resolves actual product
descriptors with cardinality one and decodes their bytes through P1. Evidence
cannot submit a product, hash, sink, or summary.

P3 owns controls `C07–C08`; their full identities come from PAPER-READY. It
participates in `E03/E05/E09/E15/E21/E24`. No evidence-authored output,
reparse, callback, fallback, dual path, CSS grammar, or Fourier edge exists.
