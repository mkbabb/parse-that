# P2 full-subject disposition

Date: 2026-07-29

Pass: `P2`

Disposition: **ROW-COMPLETE ADVERSARIAL PASS; ZERO CANDIDATES ADMITTED; FORMATION RED**

This is the second full-subject pass over the exact twenty-row P1 registry.
It binds executable challenges and terminal decisions; it is not prototype
admission, formation admission, production execution, candidate packing,
consumer proof or release.

## Frozen evidence

| ID | Coordinate | Bound object |
|---|---|---|
| `P1` | `69f72f7` | P1 registry; canonical P1 root intake SHA-256 `abc9479d7ea5fa4c76deca752428faf8b332f280e6e637de12b12b25e8e2ad6c` |
| `M2` | `de36d57dccdd20068b8c11a78f6e83d42e7d681f` | accepted closure-runtime performance control |
| `M3` | `90d4ec5` | rejected live production source; 221 additions / 264 deletions versus M2 |
| `S7` | `20b5f52` | local terminal/sequence/recovery exact-bootstrap evidence; manifest-file SHA-256 `9b9ac24e49555178c70f2e61ce69fd3f4067e0097b171954f59ad75da80af187` |
| `S8` | `27bf872` | bounded recursion evidence; manifest-file SHA-256 `ea50ec0eb643717d6645741db6166c5ad2beb65772d28bb7a45b5a85b6e61830` |
| `U` | `19ad1ac` | initial unordered tournament; manifest-file SHA-256 `378d261ae342cabc76e11b7d42de081986139d5185d8626a14b385c60260c8d0` |
| `L2` | `5822ae2` | generic source-leaf tournament; manifest-file SHA-256 `1a7a37e8af773b9888aa1f53c590c6aa994772830eae287dbef2da6e2efb89e6` |
| `SP` | `d62b73a` | shaped JSON/stylesheet products; product blob `537be68f2a1e1f8742a12e397dd28c2ec4878576`, profile blob `4804897509b687d726907e0b9e5bbc0f20ed5ef6`, manifest-file SHA-256 `ec8ebeeb1077862621be126a17f12480680f60593784ad748af25eca7969a159` |
| `UO` | `68055bda20b0f6ad20f6f73661492aa5b204c8a6` | live residual-route fixture blob `f4f4b916ba447d2ab4c6207ce1217a463369613e`, profile blob `8d08963c74fc2ee2a7f8113448ceace3a5b36937`, manifest-file SHA-256 `d9253a49c7fcbe970712cc4245711cfdb1a5891bdafb7507720cb1d033a87bfd` |
| `C` | `d323f56` | cold/hot profile blob `e837945547f4d3ff77dd92b61ffb80a19c948cbc`, result blob `b56a6b9b4291a53ebbc65854a27e9a866a994456`, manifest-file SHA-256 `850af1b66956e2b66637e1f0d49fd1a5d229397a9fd7ad4d2daf4bdd02ef879e` |
| `SURFACE` | `d323f56` read-only census | live Value, Keyframes and BBNF source/test trees contain zero direct parse-that imports; Value formation prototypes consume `/core` and `/diagnostics`; one prototype uses `skipBlockComments`; no live or second `/utils` consumer exists |
| `V2` | external formation receipt | Value R2 root `0c151de9…`, binding `a27d72a4…`, owner verification `7865862a…`; zero parser execution/product/release credit |

The Webref denominator in `C` is `@webref/css@8.7.1`: npm tarball
SHA-256 `5bab9172d75ed7eae949ea054e1a9465e213ee95004e52e8cc5705dd2278d426`,
`css.json` SHA-256
`f51f851e2f1f0ed39839f79b8dab35bb0aa398afbbcf0a110b18ac445d98aef2`,
and ordered 753-name SHA-256
`3094a4c78013d5ab853574ac0e6206c735dad5dbab9e9ba1842b1b7daab84d8a`.

## Row-complete adjudication

| Row | Full subject | P2 executable finding | Disposition | P3 challenge |
|---|---|---|---|---|
| `P2-01` | Ownership and release order | Every source transaction stayed generic and private. No CSS/domain type, dirty-head receipt, candidate pack or consumer-before-pack edge was created. | `GREEN LAW` | Reject any ownership or DAG widening; Value remains sole CSS grammar/result/UI owner. |
| `P2-02` | Archaeology and prior claims | P2 falsified the remaining broad claims on equal products: leaf widening, shaped-product fusion, residual unordered routing and compiled cold/hot dispatch. No recognizer-only or one-sided-warm result advances. | `GREEN LAW` | Re-audit P3 on exact source, data, boundary, scale and AB/BA order. |
| `P2-03` | Scalar transaction and rollback | S7, SP and UO retain one offset/value/diagnostic-length/error rollback shape with sticky faults. No second journal or token/event plane was needed. | `KEEP LAW` | Hostile same-FIRST recovery must prove frontier/evidence equality without widening the checkpoint. |
| `P2-04` | Run-scoped state and isolation | M2/S7 package and hostile tests keep diagnostics and raw memo state source/run-owned across nested parses, alternating sources and throws. | `KEEP LAW` | Re-run against the final P3 kernel; admit no parser-retained last result or global run data. |
| `P2-05` | Exact sequence slots | S7, SP and UO preserve falsy/authored slots, exact product shapes and one outer sequence result. | `KEEP LAW` | Preserve in any final closure specialization; reject implicit discard or tuple repair. |
| `P2-06` | UTF-16 positions and spans | L2, SP, UO and C cover nonzero offsets, astral/lone-surrogate edges, escapes, URL, nested calls, unordered products, EOF and non-ASCII FIRST routing. | `KEEP LAW` | Value's full denominator remains later execution proof; P3 must preserve the one numeric `{start,end}` form. |
| `P2-07` | Diagnostics and recovery | S7 and SP prove immutable successful recovery and consumer projection. UO proves recovery in a live-overlap product, but its recovery FIRST is unique. | `KEEP / HOSTILE ROUTE` | Execute same-FIRST speculative recovery under overlap; compare success frontier, labels, spans and immutable diagnostic bytes exactly. |
| `P2-08` | Result materialization | Compiler-owned `.result` remains pruned. Equal consumer projection is timed in S7/SP/UO/C and provenance survives mismatch/fault. | `KEEP LAW` | Keep projection consumer-owned and include it in every P3 timing plane. |
| `P2-09` | Literal terminal dispatch | S7's warmed high-arity prefix table is locally fast, but C's exact EOF product shows only 0.8640–3.7572× stabilized success below 753 and 1.4996× on Unicode cold edges. | `KILL COMPILED TABLE` | Incumbent source-direct closures/`dispatch` are the KISS control; no staged graph or prefix-table surface advances. |
| `P2-10` | Cold/hot dispatch | C measures construction, build-plus-first-parse, stabilized hot, alternating shapes, failure and Unicode. Construction is 0.0597–0.0785× and cold end-to-end 0.0696–0.0846×. | `RED / CLOSED` | Any P3 novelty must delete compilation cost and clear every small scale before bootstrap. |
| `P2-11` | Recursion and nesting | S8 keeps cached/mutual recursion and parse-owned typed nesting correctness. Generic recursive products are 1.212–1.331×; SP does not rescue them. Balanced-discard fusion is overfit. | `KEEP LAW; KILL FUSION` | Preserve typed pre-`RangeError` failure in the closure kernel; add no VM/trampoline absent two consumed needs. |
| `P2-12` | Memo policy | Opt-in packrat remains source-safe, re-entrant and inert until armed; JSON and Value-shaped fixtures do not consume it. No evidence supports default memoization or a registry. | `HOLD OPT-IN / PRUNE UNLESS CONSUMED` | P3 surface census must name two live consumers or exclude packrat from the successor. |
| `P2-13` | Unordered `&&` / `\|\|` | S is slower and has no routed distinction. D's disjoint ceiling misses a binding 9.559× point; UO's live residual path is only 0.7362–2.2994× with larger stable grammar heap. | `KILL S AND D` | No unordered runtime surface advances. Reopen only from an exact Value+JSON reusable need, never by retiming these families. |
| `P2-14` | Generic CSS-needed leaves | L2's callback, ASCII table and sticky wrapper are 0.0718–1.3079× on success. SP proves complete products do not recover the loss. | `KILL WIDENINGS; KEEP INCUMBENT` | Retain native sticky RegExp and ordinary source-direct closures; add only a two-consumer, full-product fusion. |
| `P2-15` | Allocation and retained heap | U/UO/L2/SP generally enlarge candidate grammar heap. C's compiled table becomes smaller at large scales, but its construction and small-scale speed are terminally RED. No allocation result rescues a candidate. | `RED / NO CANDIDATE` | Measure two memory controls on the final P3 kernel; no modeled allocation count or negative heap sample earns credit. |
| `P2-16` | IC, hidden classes, deopt and GC | S7's isolated hot terminal ICs are locally clean. S8/U/UO/L2/SP/C traces are mixed or contain candidate deopts; C is GC/table-build dominated. | `RED / NO CANDIDATE` | Seal hot-success, diagnostic and failure traces separately only after P3 has a surviving equal-product kernel. |
| `P2-17` | Isolated Value-shaped product | SP supplies a reproducible domain-neutral stylesheet-shaped product with spans, escapes, numbers, dimensions, strings, URL, nested calls and opaque recovery. It is 1.0937–1.2920× and kills the staged family. | `KEEP FIXTURE; PERFORMANCE RED` | P3 must use this fixture unchanged or bind a strict superset; it is not a live Value receipt. |
| `P2-18` | Isolated JSON-shaped product | SP passes the frozen 33-valid/7-invalid JSON corpus with equal values/failures. Candidate ratios are 0.8728–1.0021×. | `KEEP FIXTURE; PERFORMANCE RED` | P3 must use the same corpus and generic primitives; no JSON-specialized parallel engine. |
| `P2-19` | Public surface and pruning | P2 added zero exports. `/utils` has zero live source consumers and only one Value prototype use; packrat has no live Value/JSON consumer. The required two-consumer proof is absent. | `PRUNE ROUTE` | The successor excludes `/utils` and unconsumed packrat helpers unless exact immutable consumer receipts prove two uses; no compatibility alias. |
| `P2-20` | Full CSS, WPT/browser and UI | These remain Value-owned execution obligations after formation admission and candidate packing. V2 supplies formation context only. | `ROUTED / ZERO CREDIT` | Accept only the exact candidate-tarball Value receipt, full denominator/WPT/browser proof and Keyframes-through-Value migration. |

## P2 ruling

P2 is row-complete as an adversarial pass. It retires every implemented
replacement family:

- staged compiled graph, prefix table and source-leaf seam;
- balanced-discard fusion;
- unordered S and D;
- callback, declarative ASCII and public sticky leaf widenings.

What survives is smaller than a runtime candidate:

- the incumbent closure runtime is the sole executable performance control;
- one scalar transaction, run-owned state, exact slots/spans, immutable
  recovery and consumer-owned result projection survive as behavioral laws;
- native sticky RegExp and existing direct `dispatch` remain the KISS leaf
  mechanisms;
- shaped stylesheet and JSON corpora survive as equal-product fixtures;
- packrat and `/utils` are excluded from a successor absent two consumers.

No private prototype clears the every-scale, every-result ≥10× criterion.
There is therefore no formation candidate to freeze, no reason to bootstrap
the RED P2-C/UO/SP/L2 rows, and no basis for execution or release.

P3 is the next full-subject pass. It may not recombine killed mechanisms under
new names. Its first executable boundary is the same-FIRST recovery hostile
case plus a KISS closure-kernel surface cut: determine whether the surviving
laws can be expressed by direct in-place consolidation with net deletion and
without a compiler, unordered engine, generic source leaf, second result API,
packrat default or `/utils`.
