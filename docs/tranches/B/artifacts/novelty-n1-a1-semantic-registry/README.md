# N1-A1 semantic-registry correction receipt

This source-only packet corrects the vacuous collection semantics in immutable
N1-A commit `a04a7e658bee32a1fb252a4e5b4f3ce359a5947d`. It is not an N2 prototype,
benchmark, runtime, CSS implementation, candidate, package, or release.

The sole production validator is
`../../coordination/NOVELTY-EVIDENCE-REGISTRY.semantic.mjs`. Its trusted caller
pins the N1-A branch/head, N2 phase, absent N2 root, executable family, law
compatibility, packet counts, and registry/raw inventories outside submitted
records. The schema-valid gold packet has ten records. The hostile runner calls
that validator for the gold packet, a reversed-order control, and 48 one-owner
mutants; expected error codes exist only as test literals.

Run only the source validation:

```sh
node docs/tranches/B/coordination/NOVELTY-EVIDENCE-REGISTRY.semantic.test.mjs
```

The frozen N2 path remains absent. N2 dispatch remains blocked until a fresh
independent audit accepts this correction.
