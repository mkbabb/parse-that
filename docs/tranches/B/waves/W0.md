# B.W0 - Candidate-to-Release Closure

**Name**: W0 - Candidate-to-Release Closure

**Opens after**: Tranche B formation archaeology and Pass 1

**Agents**: 1 serial writer; bounded disjoint research, prototype and critic
seats may operate in isolated worktrees

**Hard gate**: One immutable candidate is consumed by Value and logical
`parse-that#jsonParser`, clears all equivalence/deletion/≥10×
CI-low/audit gates, releases, and survives the Value released-coordinate
rebind

**Status**: `in_progress`

## Goal criterion

This wave succeeds when private parser research converges into one smaller
generic runtime, two real consumers delete their displaced mechanisms, and
the exact consumed object becomes the released parse-that successor without
a scanner/token plane or parallel runtime.

## Scope

1. Reconcile M1/M2/M3 and S-family claims against hostile audits; withdraw
   stale advancement and performance language.
2. Prototype the minimum source-direct graph execution needed for exact
   sequence, slots, spans, recovery, diagnostics, bounded recursion, memo
   policy, unordered composition and source projection.
3. Profile equal products through equal boundaries at frozen small and full
   scales, with AB/BA batches, exact-bootstrap CI-low, allocation, CPU,
   hidden-class/IC, deopt and GC evidence.
4. Complete ordered full-subject P1, P2 and P3 passes; prove the isolated
   prototype at every frozen scale and result plane; and pass formation Clean
   A and Clean B. Formation admission creates zero execution credit.
5. Freeze a locally correct and reproducible production-source state, then pack one
   immutable unpublished candidate before any consumer receipt.
6. Have Value and logical `parse-that#jsonParser` consume that exact tarball
   SHA.
   Value alone owns the scannerless full CSS Syntax, selectors, at-rules,
   VDS, Webref and named-extension grammar.
7. Collect consumer equivalence, deletion and formal performance receipts;
   run execution Clean A and Clean B; release; and verify Value's
   released-coordinate rebind.
8. Prove two consumers for every generic export or prune it; `/utils` is an
   explicit terminal decision.
9. Send BBNF one frozen parse-that ABI handoff after Value
   `V.L6.css-path-abi-freeze` only.

## Trigger handling

The following conditions invalidate the current local edit boundary:

- production file bounds expand beyond the named generic runtime files;
- equal-result or ≥10× CI-low fails for a third distinct design iteration;
- CSS-specific types, trivia, recovery or registry machinery appears in
  parse-that;
- a candidate needs a fallback interpreter, tokenizer, scanner facade or raw
  alias;
- Value or logical `parse-that#jsonParser` cannot consume the exact same
  immutable pack;
- a third diagnostic iteration cannot isolate a correctness or performance
  cause.

The serial writer records the trigger and keeps the gate RED. Bounded
read-only, prototype and critic seats may investigate in isolated worktrees;
no seat may write an overlapping path or mutate the integration branch. A
genuine authority or environmental blocker returns to the user.

After P3, no new implementation campaign begins from a mechanism name alone.
The same generic primitive must have exact Value-shaped and logical
`parse-that#jsonParser`-shaped needs, predict the missing whole-product gain,
and already clear `>=10x` on a small-scale equal immutable-result preflight.
P1/P2/P3 exhausted every legal family, and sealed P3 call-tree costs already
overrun the 10× budget before omitted semantics. Root P4 reopen `d1df0f01…`
binds the user's instruction to continue without changing those laws. P4 may
challenge the bound only through isolated, mechanistically new two-consumer
research; product source and downstream edges remain blocked.

Owner adjudication `270feca` closes P4 as `REJECT/FOLD`: Sol's three designs
were unimplemented, Luna's substituted F1/F2/F3 mechanisms lose on every one
of 432 genuine JSON rows, and Luna's 432 CSS rows exercise none of them.
Corrected jury A1 manifest `3297c808…` preserves that result. Root A1 intake
`709dcac8…` dispatched one isolated Luna `P5-EUW` preflight. Luna manifest
`e2d8fb3e…` contains a correct accepted-M2/raw-consumer census but no
executable runtime. Owner evidence `0bc0d37` corrects its legacy-signature
premise and executably kills both legal clean-break variants: cursor
0.6076–0.7527× and same-state 0.6149–0.7001× across 14 fresh process rows.
P5 is `KILL/PRUNE`.

The first `P6-SIR` packet is pre-timing REJECT at owner evidence `75d36ab`:
`sepBy` masks sticky `-2` at the first element, separator and later element.
Its sealed 0.9564–1.1820× raw rows are arithmetic evidence about the defective
packet only and cannot kill the conforming atom. Preserve those bytes.
A1 owner evidence `4f772fd` rejects that repair's incomplete checkpoint and
oracle: suggestion/secondary counts and recovery-sync shadow/frontier state
drift, direct/nested typed rows are unasserted, and accepted M2 self-compares
as “native.” Its sealed 0.8716–1.0327× raw rows are likewise inadmissible.
A2 owner evidence `c9d8be1` confirms 21/21 typed-fault positions and the real
native oracle, but six of 23 routine-mismatch rows lose accepted-M2 failure
offsets. Root `trim` and `trim.or(failing-right)` produce identical signed
return/existing run state while M2 requires offsets `1` and `0`; frontier
cannot repair the projection. Its sealed 0.8991–1.1562× rows are likewise
inadmissible.

A3 owner evidence `3ed4c43` binds the conforming result. One non-control
`failureOffset` key closes all 21 typed and 23 ordinary rows plus 4/4 native
products. Fresh Sol independently admits the seven raw rows, all only
0.9208–1.0390×. P6 is terminal `KILL/PRUNE`.

There is no same-atom continuation and no distinct P7 under the current cost
law. Do not dispatch an implementation from a renamed carrier, return ABI,
checkpoint, builder, regular-expression fusion, or consumer fixture. Future
research must first quantify a genuinely new reached cost-removal edge outside
P1–P6. Product source and every downstream edge remain blocked.

Owner evidence `40d8b4c`,
`audit/POST-P6-NEXT-EDGE-OWNER-AGGLOMERATION-2026-07-30.md`, banks the required
post-P6 Sol/Luna/Sol search. Its terminal scope is reached P1–P6/current Node
26/V8 14.6 only: no current host edge survives the unchanged scannerless,
equal-product, ownership, and 10× laws. Luna's timed arms used unequal
fixture-selection streams despite equal per-fixture products; its ratios are
descriptive only. Final Sol borrows no Luna timing credit and seals
`NO-EDGE` from the primary genealogy, profile arithmetic, and equal-boundary
native yardstick.

W0 remains RED and authorizes no implementation. Resume only on a concrete
newly reachable host primitive with a complete-product model at or below
187,063 µs and a smallest equal-selection fatal assay, or on an explicit user
law amendment.

## File bounds

| File | Access |
|---|---|
| `typescript/test/prototypes/**` | create, modify, delete |
| `typescript/test/**` | create, modify |
| `typescript/src/parse/parser.ts` | modify-carve after consumer proof |
| `typescript/src/parse/state.ts` | modify-carve after consumer proof |
| `typescript/src/parse/leaf.ts` | modify-carve after consumer proof |
| `typescript/src/parse/lazy.ts` | modify-carve after consumer proof |
| `typescript/src/parse/packrat.ts` | modify-carve after consumer proof |
| `typescript/src/parse/utils.ts` | modify-carve or delete after consumer proof |
| `typescript/src/parse/parsers/json.ts` | modify-carve as named non-CSS consumer |
| package barrels/manifests | modify only for final prune/release |
| `docs/tranches/B/**` | create, modify |

Do not touch Value CSS grammar/UI, Keyframes, BBNF, protected worktrees, or
the untracked `data` symlink from this wave.

## Disjointness

The wave has one serial integration writer. Bounded research, prototype and
critic seats may work concurrently only on disjoint paths or read-only
surfaces. Value, Keyframes and BBNF remain external writer domains; only
immutable receipts cross the boundary.

## Worktree plan

The integration worktree is
`/Users/mkbabb/Programming/parse-that-css-totality`. Candidate, baseline,
consumer and critic work may use named isolated worktrees after file bounds
are recorded. Only the serial writer integrates. Temporary detached
baselines are read-only and are removed after their hashes and outputs are
sealed.

## Agent unit

### B.W0.1 Candidate-to-release runtime

- **Goal:** One generic source-direct runtime satisfies the Value CSS and
  logical `parse-that#jsonParser` obligations with net consolidation and no
  second parser surface.
- **Mechanism:** Iterate private incompatible kernels; reject on semantic,
  result, memory or CI-low failure; form one immutable pack only after a
  single mechanism survives.
- **Files:** The wave file bounds above.
- **Sub-gate:** Every hard gate below resolves to a source hash, command,
  raw artefact, consumer diff/receipt, pack SHA or released-coordinate
  receipt.

## Hard gate

1. `npx tsc --noEmit --project tsconfig.json`, focused prototype suites,
   package suites and build all pass with exact pass/skip counts.
2. Hostile vectors prove exact slots including
   `[undefined, false, 0, "", span]`, complete scalar rollback, monotone
   frontier/work/max-depth, diagnostic truncation and run isolation.
3. Bounded recursion yields a typed fault before host `RangeError`; raw memo
   cells cannot leak across sources/runs.
4. Unordered composition proves deterministic `&&`/`||` behavior without
   state explosion above the declared cap.
5. Full-subject P1, P2 and P3 complete in order; an isolated prototype clears
   every frozen scale and result plane; formation Clean A and Clean B accept
   the same evidence before formation admission.
6. One immutable unpublished candidate tarball is sealed to the frozen source
   commit, file manifest and SHA-256 before either consumer receipt.
7. Value's sole source-direct CSS grammar consumes that exact SHA and covers
   the frozen standards denominator and named extensions with WPT/browser
   differentials.
8. Logical `parse-that#jsonParser` consumes the same exact SHA and matches
   frozen JSON vectors and `JSON.parse` products.
9. Both consumer diffs prove net deletion/consolidation; no token/scanner,
   serializer, alias, shim or fallback path remains.
10. At every declared scale, the exact-bootstrap 95% low is ≥10× for equal
   values, spans, diagnostics, recovery and failure behavior. Result
   materialization is timed, not excluded.
11. CPU, heap, allocation, hidden-class/IC, optimization/deoptimization and
   GC outputs are sealed to source, data, Node/V8, order and batch inputs.
12. Execution Clean A and Clean B accept the same source, immutable
    unpublished candidate tarball and consumer evidence.
13. `/utils` has two exact consumer receipts or is absent from the pack.
14. The release tag/tarball matches the admitted commit; Value rebinds to
    the released coordinate and reruns its full gates green.
15. Parse-that sends BBNF only the frozen generic ABI handoff after
    `V.L6.css-path-abi-freeze`.
16. `git diff --check`, manifest verification, clean tracked status and the
    integrity sweep are green.

## Format and lint cadence

After each banked implementation slice and at wave close:

```sh
npx tsc --noEmit --project tsconfig.json
npx vitest run
npm run build
git diff --check
```

Run repository proof scripts and format/lint commands present in
`package.json`; record an unavailable command rather than inventing one.
Benchmark/profile artefacts are regenerated only from the sealed source.

## Verification artefacts

- `docs/tranches/B/audit/PASS-3-S-EVIDENCE-AMENDMENT-2026-07-29.md`
- `docs/tranches/B/audit/P6-SIR-A2-OWNER-SOL-ADJUDICATION-2026-07-30.md`
- `docs/tranches/B/audit/P6-SIR-A3-OWNER-SOL-ADJUDICATION-2026-07-30.md`
- `docs/tranches/B/audit/POST-P6-NEXT-EDGE-OWNER-AGGLOMERATION-2026-07-30.md`
- `docs/tranches/B/audit/ACTIVE-GOAL-COMPLETION-AND-PRECEPTS-MATRIX-2026-07-30.md`
- post-P6 Sol research root
  `/Users/mkbabb/Documents/Codex/2026-07-30/parser-next-edge-sol-research/outputs`,
  manifest SHA-256 `2f626edb7e0d5e323eb82db1a933e917f907713dcd7ab942dc38cedad1ed0ce3`
- post-P6 Luna measurement root
  `/Users/mkbabb/Documents/Codex/2026-07-30/parser-next-edge-luna-research/outputs`,
  manifest SHA-256 `2b35d78348254ac66c9fcbb3c0d398a54bba9d37cb4f4e89dbda049c984a7f6d`
  and classification `UNEQUAL_SELECTION_CONSERVATIVE_MEASUREMENT_EVIDENCE`
- final Sol agglomeration root
  `/Users/mkbabb/Documents/Codex/2026-07-30/parser-next-edge-sol-agglomeration/outputs`,
  manifest SHA-256 `62e6d344b5f23e340e58a9cbed524f5ab50f2200c4040793450c5d6cfb32b09e`
- `docs/tranches/B/artifacts/**`
- candidate `npm pack` SHA-256 and file list
- Value and logical `parse-that#jsonParser` consumption/deletion receipts
- WPT/browser differential outputs owned by Value
- two hostile audit reports
- release tag, tarball SHA and Value released-coordinate receipt
- `docs/tranches/B/FINAL.md`

## Commit plan

1. `docs(parser-tranche): form canonical candidate-to-release authority and
   retract invalid S admission claims`
2. `perf(runtime-prototype): bank each evidence-complete private mechanism`
3. `fix(runtime/<mechanism>): freeze the locally correct,
   net-consolidating source cut`
4. `build(candidate-pack): seal the immutable unpublished tarball and
   manifest`
5. `refactor(json-consumer): consume the exact candidate SHA and delete
   displaced paths`; Value returns its own exact-SHA receipt
6. `perf(runtime-admission): seal consumer-equivalent CI-low and V8 evidence`
7. `release(runtime): bind the accepted pack, audits and release`
8. `docs(parser-tranche): close after the Value released-coordinate rebind`

Every broad, benchmark, profiling, deletion, gate and release commit carries
an evidence body. The untracked `data` symlink is never staged.

## Dependencies

- **Depends on:** Value formation packet; frozen Webref/CSS/WPT inputs;
  admitted parse-that formation before production-source execution; Value
  `V.L6.css-path-abi-freeze` before the parse-that → BBNF handoff.
- **Blocks:** Value V.L1/V.L5 execution and released-coordinate rebind;
  Keyframes migration through Value; the parse-that → BBNF ABI handoff after
  `V.L6.css-path-abi-freeze`.

## Archaeology

- `de36d57`: banked M2 run-local recovery/memo isolation.
- `90d4ec5`: M3 correctness with failed performance; rejected.
- `0067c31`: S1 large signal with invalid admission framing.
- `96ad567`: S2 sequence signal with invalid admission framing.
- `c480578`: corrected accepted-M2 control; every small success/result plane
  remains below ≥10× at CI-low.
- `75b76dd`: S4 direct authored spans; held below the small-scale bound.
- `926a3ad`: S5 prunes the unconsumed compiled result surface; held below the
  small-scale bound.
- `062c147`: S6 run-owned state and consumer result projection clear the
  seven-process terminal/sequence lower bound at both 96 and 753 names;
  formation remains open for the rest of the full subject and both clean
  audits.
- `20b5f52`: S7 collects immutable recovery evidence once, resets the
  frontier, preserves an O(1) run builder and clears every declared
  terminal/sequence/recovery CI-low at 96 and 753 names. At that coordinate,
  recursion, unordered composition, CSS-needed leaves and full-subject
  formation passes remained open; live consumers remained later execution
  work.
- `27bf872`: S8 proves cached generic recursion, mutual recursion and typed
  parse-owned nesting correctness, but generic success/result is about
  1.2–1.3× and balanced-discard fusion remains below 9× at the binding
  96-leaf scale. The 753-leaf signal cannot waive that RED row; no formation
  or execution credit follows.
- `19ad1ac`: U proves scannerless bounded unordered correctness at 4/8/16/33
  members through 24 unordered cases plus 13 S regressions. S is retired
  because it enumerates all available arms, has no disjoint-FIRST route and
  loses to the control. D reduces attempts to one per disjoint member but
  remains performance-RED: 1.427–6.156× at 4/8/16 and one 33-member success
  order at 9.559×. All timed fixtures have `residuals: 0`; overlap
  correctness has no residual-overlap performance result.
- `69f72f7`: the 20-row hash-bound registry completes P1 reconciliation with
  no formation or later credit.
- `5822ae2`: P2-L proves 51/51 exact generic source-leaf products but only
  0.0718–1.3079× success. Callback, ASCII-table and public sticky-wrapper
  widenings are killed; one private fixture seam remains for shaped products.
- `d62b73a`: recursive JSON and domain-neutral stylesheet-shaped products
  are correctness-green, but JSON is 0.8728–1.0021×, the fixture
  1.0937–1.2920× and retained heap about 1.166× control. The staged
  full-product/source-leaf candidate is killed.
- `68055bd`: a domain-neutral mixed-overlap product forces D's live residual
  route at 4/8/16/33 members with equal slots, spans, immutable recovery and
  failures. Twenty AB/BA points are only 0.7362–2.2994× and stable candidate
  grammar heap is ~2.06–2.40× control. D is terminally retired with S; no
  unordered candidate survives. Same-FIRST speculative recovery remains a
  routed hostile case; row-complete P2 disposition was still open at that
  coordinate.
- `d323f56`: exact EOF/span cold/hot products at 4/8/16/33/96/753 put
  construction at 0.0597–0.0785×, build-plus-first-parse at
  0.0696–0.0846×, stabilized success below 10× under 753 and Unicode cold
  routing at 1.4996×. The staged graph, prefix table and compiled parser
  surface are retired.
- `c3d42d4`: all twenty P1 subjects receive exact P2 KEEP/KILL/RED/ROUTED
  dispositions. P2 is complete with zero admitted candidates. P3 begins from
  same-FIRST recovery and a direct closure-kernel surface cut; it may not
  recombine retired families.
- `f4b9abb`: all six same-FIRST recovery permutations preserve successful
  values, slots, spans and final state, but retired D changes four immutable
  recovery frontiers when diagnostics are off and converges only when they
  are enabled. Accepted-M2 traversal order is binding; D remains killed. This
  closes one hostile P3 case, not P3 or formation.
- `19c1e12`: the smallest direct scannerless closure surface preserves 36
  warmed success projections, two failure-state subsets, frozen JSON,
  bounded recovery and typed nesting. Complete state, failure-result,
  invalid-M2, cold/first, alternating-shape, bootstrap and scaled
  diagnostics-on recovery planes remain RED. Its 1.0656–1.5238× range and
  12.4% retained parse-state increase kill the candidate despite the
  source/grammar-heap cut.
- `43d7d4a`: all twenty subjects receive terminal P3 dispositions. P1/P2/P3
  are complete with zero surviving candidates; isolated proof remains RED
  and formation Clean A/B stay blocked.
- `605a1ae`: seven processes compare accepted-M2 logical `jsonParser` with
  native `JSON.parse` on equal values and frozen success envelopes. The
  binding scale-4/8 immutable-result medians are only 5.0050×/6.0826×. Native
  is a yardstick, not a candidate or admission control. Independent sealed
  P3 call-tree replay reproduces 311,883 µs of native matching plus required
  products against a 187,063 µs 10× budget. No legal family survives and the
  result remains immutable negative evidence for the user-reopened P4 loop.
- `2bc4abb2…`: root binds the P4 full-CSS denominator at 1,717 raw/1,653
  active Webref rows, including aliases/manual rows, 25 token-equivalent
  decisions, ten entry points, eleven tree algorithms, recovery/fidelity,
  WPT manifests and 53 Keyframes references across 51 files. It grants zero
  coverage or formation credit.
- `270feca`: independent owner adjudication binds corrected jury A1
  `3297c808…`, P4 `REJECT/FOLD`, Sol RSR/CISF/CTPT as unimplemented, all 432
  genuine Luna JSON mechanism rows below `1x`, and all 432 Luna CSS rows as
  same-body zero-credit evidence. No candidate, formation, product, API, or
  release edge opens.
- `709dcac8…`: root independently verifies A1 and dispatches one isolated
  Luna P5-EUW preflight.
- `0bc0d37`: owner Sol verifies Luna seal `e2d8fb3e…`, rejects legacy raw
  signature as an independent clean-break law, and kills both executable EUW
  forms below `1x`. P6-SIR is routed as unimplemented-debt falsification only.
- `75d36ab`: owner Sol binds fresh-Sol seal `78cb1aa0…` and rejects the first
  P6 packet before timing because `sepBy` turns sticky `-2` into success at
  three required positions. The original raw is immutable/inadmissible;
  P6-A1 alone may repair and retest the atom.
- `4f772fd`: owner Sol binds A1 fresh-Sol seal `55a44e09…`, rejects A1's
  incomplete checkpoint/oracle gate, preserves its raw as inadmissible, and
  routes same-seat P6-A2. No distinct P7 is admissible absent a new reached
  cost-removal edge.
- `c9d8be1`: owner Sol binds A2 fresh-Sol seal `97e72af8…`, accepts the
  complete typed/oracle correction, rejects six ordinary-failure offsets,
  preserves its raw as inadmissible, and routes one non-control scalar A3.
- `3ed4c43`: owner Sol binds A3 fresh-Sol seal `ff193de0…`, accepts the full
  semantic/mechanism plane and seven admissible 0.9208–1.0390× rows, and
  terminally kills P6. No same-atom continuation or distinct P7 remains.
- `40d8b4c`: owner Sol/Luna/Sol agglomeration seals a narrowed current-host
  `NO-EDGE` result, quarantines Luna's unequal-selection timing rows, and
  leaves the long-horizon goal active with zero downstream credit.
- XR-18: exposed the Value/parse-that release cycle and every-scale CI-low
  requirement.
- XR-21: exposed unequal boundaries, prefix shadow, duplicate labels,
  fixed order, incomplete memory controls and unsealed inputs.

The guardrail is executable: no advancement claim appears while any binding
scale, result plane, consumer receipt or audit is RED.
