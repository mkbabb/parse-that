# N2 IETM paper v1 — Review B owner intake

Date: 2026-08-02

Status: **TERMINAL AMEND/RED — PAPER ONLY — ZERO CREDIT — B2 NOT DISPATCHED**

## Frozen input

- packet commit: `e1fd16591ac850ffe102fb8287f6640fc9173e9c`;
- packet root: `docs/tranches/B/research/N2-IETM-PAPER-v1/`;
- manifest SHA-256:
  `c67b8e09e279895fcd2940b7ccb5e7b4e562303679435bd0015aabf1a2719363`;
- manifest replay: `9/9 EXACT`;
- source, prototype, Node, parser, static analysis, build, benchmark, product,
  package, review dispatch, and N2e execution: `0`.

The v1 packet remains immutable. This intake records the reported fresh Review
B ruling; it does not claim an external review-file identity that was not
supplied, modify v1, or authorize source/execution.

## Shared first exact falsifier

```text
E04_EFFECT_IDENTITY_UNCLOSED
```

Review B independently reaches Review A's first failure: P0/E04 owns only
grammar/action/environment identity while P4 assumes rule/callback/expected-
event authority. Two callbacks under the same E04 triple can yield different
effect traces. P4 must self-author the missing authority or consume a hidden
edge, so v1 is RED before hostile closure.

## Independent hostile amendments

### P0/P1 — canonical authority and byte classes

P0 must P1-decode canonical declaration and ledger bytes. `IdentityEpoch` owns
grammar, rule, action, callback, environment, and expected-event-contract bytes
and hashes. P1 must classify every byte/scalar class, including U+007F and
U+0085, and preserve exact decode→encode byte identity.

E07's v1 `rewriteCanonical` couples the codec to hostile mutation policy. V2
must replace it with codec-only decode/encode edges; P6 owns every transform
and cannot ask P1 to choose or perform an injector.

### P2/P3 — ownership of atoms and traversal

P2 must define point bias at replacement start for zero-width insertion and
nonzero replacement. It owns only coordinate atoms, edit-chain validation, and
the resulting relocation map. It cannot accept or traverse P3's product.

P3 alone owns and applies the generated exhaustive product visitor. Its schema
must enumerate exact constructor names, fields, scalar/graph types, ordering,
optionality, and cardinalities for top-level memo state and every frontier,
rollback, recovery, diagnostic, selection, slot, span, provenance, source-read,
depth, fault, and effect occurrence. It must bind typed memo-entry provenance
and an authenticated fresh-target-candidate input before comparison.

### P4/P5 — observed effects and fresh control

P4 observed events include `ruleHash` and consume no implicit row contract.
P4 receives expected identity through E04 and observed fresh-control effects
through a P5-authenticated `FreshControlReceipt`. P5 binds target complete
product, observed effects, target source/version, row/run, control artifact,
command/runtime, and semantic envelope from owner-held bytes. It supplies the
receipt separately to P3 and P4; candidate evidence cannot author either side.

### P6/P7 — closed hostile ownership

P6 must enumerate the complete `PredicateId`, `InjectorId`, injector→owner
table, byte paths/domains, and production predicate reached by every canonical-
JSON mutation. P6 owns byte-domain hostiles only. P1 supplies codec operations,
not mutation policy.

Raw-source, AST, source/prose, forbidden-edge, dependency, reflection/dynamic-
execution, module-count, and LOC mutants move to P7. P7 owns an external AST
tool pin, exact call/data/import/export graphs, AST-normalized nonblank/
noncomment logic metrics, and closed bans on dynamic import/eval/Function,
computed unowned property access, reflection, runtime module discovery, hidden
dependencies, and candidate self-verification.

All external owner, observer, harness, comparator, auditor, and verifier
modules required by the mechanism count inside the same eight-module/850-LOC
ceiling. A “test-only”, generated, helper, or owner-side label does not exclude
executable logic from the budget.

## Review B disposition

| Interface | v1 ruling | Required v2 result |
|---|---|---|
| P0 | `AMEND` | complete identity/event authority from canonical bytes |
| P1 | `AMEND` | exhaustive byte classes; codec-only edges |
| P2 | `AMEND` | replacement-start bias and coordinate-map-only output |
| P3 | `RED` | closed constructors/cardinalities; sole visitor; fresh target |
| P4 | `RED` | ruleHash plus P5 fresh-control effects |
| P5 | `AMEND` | authenticated FreshControlReceipt to P3/P4 |
| P6 | `AMEND` | closed IDs/ownership; canonical-byte hostiles only |
| P7 | `AMEND` | source/AST/budget hostiles and closed dynamic bans |

Every v2 edge row must be byte-identical in both owning contracts. A one-sided
edge, hidden reverse callback, or implicit expected value is RED. All eight
contracts, manifest, and receipt move atomically; no partial v1 amendment or
mixed v1/v2 review packet exists.

## Exact boundary

- Review B terminal result: `AMEND/RED`;
- scientific/equivalence/timing/novelty/law/product/downstream credit: `0`;
- v1 mutation or reseal: `FORBIDDEN`;
- source/prototype/N2e execution: `WITHHELD`;
- Review B2: `NOT DISPATCHED`;
- next action: author one immutable ten-file paper v2 resolving Review A and B
  atomically, freeze/hash it, then hold for fresh independent A2/B2.
