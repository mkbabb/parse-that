# Post-P6 reached-edge owner agglomeration

Date: 2026-07-30

Status: **CURRENT-HOST NO-EDGE — RESEARCH CLOSED — FORMATION RED — GOAL
ACTIVE — ZERO CREDIT — NO RELEASE**

## Ruling

P6-A3 is the terminal conforming signed-step assay. It preserves the required
state/value/result semantics, but its seven admissible rows are only
`0.9207681564329404x–1.0389571550178254x`; P6 remains `KILL/PRUNE`.

The required post-P6 Sol/Luna/Sol search found no genuinely new reached
cost-removal edge outside P1–P6 in the supported Node `26.0.0` / V8
`14.6.202.33-node.19` host. This is a reached/current-host feasibility ruling,
not a universal impossibility theorem. It authorizes no new parser family,
prototype, production change, CSS work, candidate pack, API, release, or
downstream credit.

The long-horizon goal remains active. Formation is RED because no isolated
candidate clears the unchanged scannerless, equal-product, and `>=10x`
CI-low laws. Full CSS coverage remains a later binding Value-owned execution
criterion and has earned zero coverage.

## Bound predecessor

- terminal P6 owner commit:
  `3ed4c433cc61772fc202d380c3741b0b7bb0682a`;
- terminal P6 owner document:
  `docs/tranches/B/audit/P6-SIR-A3-OWNER-SOL-ADJUDICATION-2026-07-30.md`;
- terminal P6 owner-document SHA-256:
  `ab3b34cd1acdbca409595fcab893ce6965fa68c946fe91d20dfc684c230f6a16`;
- canonical P6 reconciliation:
  `fe4e3a57af58465f59487f3053df49e572aa1fca`;
- accepted-M2 control:
  `de36d57dccdd20068b8c11a78f6e83d42e7d681f`; and
- full-CSS denominator SHA-256:
  `2bc4abb218d3e9c8aba4164ca1c48df7cb714222b024290b60dc7e239e26433b`.

The owner commit is an ancestor of the canonical reconciliation, the
owner-document bytes equal the commit blob, and the tracked tree was clean at
intake except for the protected untracked `data` symlink.

## Sol architecture packet

Root:
`/Users/mkbabb/Documents/Codex/2026-07-30/parser-next-edge-sol-research/outputs`

The root contains exactly four regular files and no directories. Its manifest
SHA-256 is
`2f626edb7e0d5e323eb82db1a933e917f907713dcd7ab942dc38cedad1ed0ce3`;
all three entries verified twice:

| File | SHA-256 |
|---|---|
| `EDGE-ARCHITECTURE.md` | `95a70b9a0068c47c9654a4855e35cbe1aad378ff6abc19dde41cd26958972ea9` |
| `GENEALOGY.json` | `bea94ff1eb4083aaec662c9451e5f1f5e6306e8071f1930adfae6e76ac9546fe` |
| `COST-MODEL.json` | `c324fedd7b538b71abd83ac05cdffe1199ad945e436a459b57aa4527f996d144` |

Both JSON files parse. Root independently reproduced the bound P1/P2/P3,
accepted-M2, P3 profile/log, native-yardstick, and full-CSS identities.

Sol maps every currently reachable host route to either incumbent work, a
killed P1–P6 family, or an unchanged forbidden boundary:

- direct code-unit/literal operations are already incumbent;
- bulk search does not provide grammar, recovery, or provenance;
- capture/RegExp variants recur to P4;
- typed storage, regions, and product fusion recur to P3/P4;
- exceptional mismatch and signed carriers are killed by P5/P6;
- native JSON is a specialized alternate executor, not a generic CSS
  combinator edge;
- scanner/token/compiler/VM/generated grammar, raw alias, omitted product,
  cache-only, second-source, and alternate-path forms remain forbidden.

## Luna measurement packet and defect

Root:
`/Users/mkbabb/Documents/Codex/2026-07-30/parser-next-edge-luna-research/outputs`

The root contains exactly four regular files and no directories. Its manifest
SHA-256 is
`2b35d78348254ac66c9fcbb3c0d398a54bba9d37cb4f4e89dbda049c984a7f6d`;
all three entries verified twice:

| File | SHA-256 |
|---|---|
| `EDGE-ASSAY.md` | `d4fb6a6865ca98c0d8ee2406d2e7b572d4258896a4a7ff25020d1f722c5d03b5` |
| `FALSIFICATION.json` | `d23537475a2948c9b91596fe8cf824d5857a14418102e343a2cffe4c332bf9a7` |
| `RAW.json` | `f74a4ac5619821c4d6dda01d0ade4ea4046718fba2a34ae8865b9bbc5eb54872` |

Root and final Sol reproduced:

- seven unique processes and seeds;
- `35/35` per-process/suite equality flags;
- `35/35` frozen flags;
- `315/315` per-fixture product vectors equal across arms;
- `133/133` hot-batch sums equal to recorded totals;
- all 57 ratio vectors; and
- all 57 ordered `7^7` bootstrap distributions, or 46,941,951 recomputed
  values, with zero arithmetic mismatches.

The packet is nevertheless not equal-selection evidence. Aggregate hot sinks
diverge in `35/35` process/suite cells, and cross-arm batch sinks diverge in
`350/350` process/suite/batch cells. Equal per-fixture products plus divergent
timed sinks prove that the arms consumed different fixture-selection streams.
The `equal sinks` statement in `EDGE-ASSAY.md` and
`FALSIFICATION.json.protocol.equalSinks: true` are false.

Luna is therefore classified only as
`UNEQUAL_SELECTION_CONSERVATIVE_MEASUREMENT_EVIDENCE`. Its strongest
host-only `5.945344642725845x` CI-low, complete-product
`2.09820037535609x` CI-low, and optimistic `191686.89882143293 us`
extrapolation are descriptive only. They receive no equal-work or formation
credit.

A future attempt to reuse Luna's ratios must regenerate one identical
fixture-index vector per process/suite/batch, reset every arm to that vector,
and require both per-batch and aggregate sink equality before ratios or
bootstrap. The first unequal sink/product, unfrozen product, arithmetic
mismatch, or sub-`10x` CI-low stops the packet. No correction replay is
required for the present ruling because no binding claim borrows Luna data.

## Third-pass Sol agglomeration

Root:
`/Users/mkbabb/Documents/Codex/2026-07-30/parser-next-edge-sol-agglomeration/outputs`

The root contains exactly four regular files and no directories. Its manifest
SHA-256 is
`62e6d344b5f23e340e58a9cbed524f5ab50f2200c4040793450c5d6cfb32b09e`;
all three entries verified twice:

| File | SHA-256 |
|---|---|
| `ADJUDICATION.md` | `9f722210e92289a0590890be0ded808283a5ed335b873c131d6ebb1dfe3335a5` |
| `PACKET-AUDIT.json` | `de59351d66485f17d4f9f0283862a3b6fb0b981cbe5b71cb7bbb82d58c6c2c32` |
| `DECISION.json` | `202a6dad8b12a35e8303bf3ecc39f063ae83b0bb3ff98f65f904a9ce09afb6df` |

Both JSON files parse. The terminal choice is `SEAL_NO_EDGE`. Sol borrows no
binding claim from Luna and bases the narrowed ruling on the P1–P6 inverse
genealogy, primary P3 profile arithmetic, and equal-boundary native JSON
yardstick.

## Binding arithmetic

The primary P3 profile and current-host equal-boundary yardstick remain the
authority:

```text
10x whole-work budget                 187,063 us
candidate native matching             144,501 us
required immutable products           167,382 us
matching + required products          311,883 us
budget overrun                         124,820 us
budget left after products              19,681 us
matching speedup if products fixed    7.3421574107006755x
matching work removed if fixed        86.38002505172975%
```

The product and matching buckets are observed profile attribution, not
physical lower bounds. A lawful future edge may fuse them only while retaining
all equal values, UTF-16 spans, diagnostics, recovery, failure behavior, and
immutable materialization inside the measured boundary. P4 already assayed
the reached action/region/capture fusion families and killed them.

The equal-boundary native JSON yardstick remains below `10x` at all six
scales. Immutable-result CI-low ranges from
`4.9512716316959775x` at scale 4 to `7.555042329173102x` at scale 753. Even
this JSON-specialized native path does not clear the threshold before generic
CSS/combinator identity and the omitted failure/recovery obligations.

## Ownership and credit

Ownership is unchanged:

- parse-that owns only generic source-direct combinators, runtime state,
  UTF-16 spans, recovery, diagnostics, typed faults, recursion, and
  performance;
- Value owns the sole CSS grammar, CSS consumer, canonical inverses,
  transform/path results, and UI;
- Keyframes consumes Value and deletes local grammar/scanner/serializer forks
  only after the Value surface exists; and
- BBNF receives the frozen parse-that ABI only after
  `V.L6.css-path-abi-freeze`.

This research grants zero novelty, formation, source, API, product, CSS,
Value, Keyframes, BBNF, execution, admission, packing, or release credit.
`NO RELEASE` remains binding.

## Exact resume boundary

Do not dispatch a renamed P7, another carrier, table, regex, clone, buffer,
cache, native JSON path, or primitive-only speed claim.

Resume implementation only after either:

1. a concrete primitive newly reachable from the supported JavaScript runtime
   has primary API/implementation evidence that it removes reached
   grammar-path and required-product work rather than renaming P1–P6, and a
   conservative full-boundary model totals at or below `187,063 us` while
   including exact values, spans, diagnostics, recovery, failure,
   materialization, cold/first, hot, generic JSON, and Value-owned CSS; or
2. the user explicitly amends the scannerless, equal-product, ownership, or
   `>=10x` law.

Any future authorized mechanism begins with the smallest equal-selection,
balanced AB/BA fatal cell and stops at the first unequal sink/product or
sub-`10x` CI-low. Full CSS, formation Clean A/B, production freeze, immutable
candidate packing, Value plus logical `parse-that#jsonParser` consumption,
release, Value rebind, ABI freeze, and BBNF remain blocked.
