# Novelty N2-F0 terminal RED owner intake and N2a ruling

Date: 2026-08-01

Status: **N2-F0 TERMINAL RED — MECHANICS FAILURE — ZERO CREDIT — N2A SAME-TASK FRESH-ROOT DISPATCH AUTHORIZED, NOT SENT**

## Scope and preserved authority

This is a tranche-development intake only. It records the failed first
invocation of the sole N-IETM Luna task, freezes every byte that remains from
that attempt, and defines the only legal correction boundary. It changes no
parser source, public API, Value-owned CSS source, package, candidate, release,
or consumer coordinate.

- repository: `/Users/mkbabb/Programming/parse-that-css-totality`;
- branch: `codex/css-totality-combinators-20260729`;
- owner predecessor HEAD:
  `0cd7eda8032197fd7c2c2b9a229c019610da2508`;
- N1-A4 authority commit:
  `81b2a0aacebc126ac1a093407cb2e24ed5d78ee8`;
- sole Luna task:
  `019fb12c-2624-7732-909a-e1e87ce7ef24`;
- failed root:
  `/Users/mkbabb/Documents/Codex/2026-08-01/parser-novelty-n2-ietm-f0`;
- proposed correction root:
  `/Users/mkbabb/Documents/Codex/2026-08-01/parser-novelty-n2a-ietm-f0`.

The proposed correction root was absent at owner census. This document does
not create it and does not send or resume a task.

## Failure and chronology ruling

The first recorded invocation was:

```text
node tests/f0-probe.test.mjs
```

It exited `1` with `MODULE_NOT_FOUND` for package specifier `ohm-js`, raised
from `source/f0-probe.mjs`. No scientific probe, sealed result, timing,
manifest, checksum set, or gate result followed. The executor proposed a
same-root correction; owner STOP prohibited it.

The current `source/f0-probe.mjs` bytes are not the first-invocation bytes.
They now contain absolute Ohm runtime and package paths, their modification
and change times postdate root creation, and no pre-patch source hash was
preserved. The current source contains no `/tmp` write site even though an
outside-root raw file exists. Therefore neither the current source nor that
residue can reconstruct a trustworthy pre-failure chronology. The root is
terminal mechanics archaeology, not a prototype packet.

## Independently reproduced frozen census

The root has exactly five child directories, three regular files, no
symlinks, no special nodes, and `16,642` regular-file bytes. `raw/` and
`results/` are empty. There is no README, result, receipt, manifest, or
checksum file.

Root stat at census:

```text
size=224 mode=drwxr-xr-x nlink=7
birth=2026-08-01T23:46:36-0400
ctime=2026-08-01T23:50:17-0400
mtime=2026-08-01T23:50:16-0400
```

| Path | Bytes | Mode / links | SHA-256 | Birth / change / modify |
|---|---:|---|---|---|
| `fixtures/falsification-matrix.json` | 1,016 | `0644` / `1` | `bcc01b9106d2c5a6ed60d8b089eca9d1e541bbd2528f57a4ef22358ecbca279b` | `23:48:44` / `23:50:13` / `23:48:44` |
| `source/f0-probe.mjs` | 15,044 | `0644` / `1` | `3e381cd20375a5858e488f4bc1a2867e95ece5036d315f19bdb641b83ac260c1` | `23:48:44` / `23:50:17` / `23:49:22` |
| `tests/f0-probe.test.mjs` | 582 | `0644` / `1` | `b580c8f8069bdaddc1a3fd3c87c20bbea2557444d95723f4286b302daffb5ed8` | `23:48:44` / `23:50:13` / `23:48:44` |

Times in the table are on 2026-08-01 at UTC-04:00. The differing source
birth/change/modify tuple is evidence of post-creation activity, not proof of
which bytes ran first.

Outside-root residue is preserved in place and must not be cleaned, moved,
rewritten, or adopted as sealed raw evidence:

| Path | Bytes | Mode / links | SHA-256 | Birth / change / modify |
|---|---:|---|---|---|
| `/tmp/f0-raw.ndjson` | 18,478 | `0644` / `1` | `e221e630bd6c955fe61b5b4ca540e36ee92e8137febe11e96245c8cd22b2692e` | `23:48:49` / `23:49:27` / `23:49:27` |

## Terminal disposition and credit

| Plane | Disposition |
|---|---|
| root reuse, repair, rerun, reseal, cleanup | `FORBIDDEN` |
| mechanics | `RED: brittle package-name resolution on first invocation` |
| source chronology | `RED: first-run bytes unbound; later source mutation proven` |
| raw/result authority | `RED: no in-root raw/result/receipt/manifest/checksum` |
| prior-art and fatal gates | `NOT RUN / NO CREDIT` |
| product equality and mechanism reach | `NOT RUN / NO CREDIT` |
| timing, allocation, GC, deopt, IC, package cost | `NOT RUN / NO CREDIT` |
| PL-BE, PL-2X, PL-3X, historical 10x | `0` |
| parser runtime, CSS, Value, Keyframes, BBNF, candidate, API, package, release | `0` |

N2-F0 is one terminal mechanics root and zero scientific N2 receipts. It does
not advance N2, N3, any law family, formation, execution, or release.

## Pinned Ohm inputs for the only legal correction

Before the successor root is created, the owner/executor boundary must verify
these exact existing regular files without package-name or working-directory
resolution:

| Input | Bytes | Mode / links | SHA-256 | Contract |
|---|---:|---|---|---|
| `/Users/mkbabb/Programming/parse-that-css-totality/typescript/node_modules/ohm-js/dist/ohm.cjs` | 192,483 | `0644` / `1` | `8c8c6544103f6e9b5361f3d462f1929f2524993d2c0dc1464fa08dc5ce3e5b34` | absolute CommonJS runtime input |
| `/Users/mkbabb/Programming/parse-that-css-totality/typescript/node_modules/ohm-js/package.json` | 5,235 | `0644` / `1` | `9cff75d790c89c4a1d8eab1ae6a555cb491281d4d3307fc811294cda94d03350` | exact version input: `17.5.0` |

Owner read-only resolution under Node `v26.0.0` loaded the absolute runtime,
observed callable `grammar`, and reproduced package version `17.5.0`. That
check is an input-pin diagnostic only; it is not N2 preflight or prototype
credit.

The successor may not use `require('ohm-js')`, `import 'ohm-js'`,
`require.resolve('ohm-js')`, `NODE_PATH`, current-working-directory discovery,
or an unpinned `node_modules` search. Any hash, size, file-type, link-count,
version, or absolute-load mismatch is the first RED and stops before root
creation.

## N2a same-task fresh-root dispatch law

Exactly one correction attempt is authorized, but has not been dispatched.
It must use the same task `019fb12c-2624-7732-909a-e1e87ce7ef24`; no new
task, owner lineage, sibling executor, or duplicate root is permitted.

The order is binding:

1. Re-census N2-F0 and `/tmp/f0-raw.ndjson`; any identity drift stops.
2. Verify that the N2a root is absent.
3. Verify both pinned Ohm inputs exactly and load only the absolute runtime.
4. Materialize the new N2a root once. Record root birth before any source
   write.
5. Place every temporary, raw, result, log, stderr, receipt, and checksum path
   under the N2a root. Set `TMPDIR` to a root-contained directory. Forbid
   `/tmp`, `os.tmpdir()` fallback, and any absolute output path outside the
   root. A lexical plus resolved containment audit must run before execution,
   and a post-run inventory must reject every outside-root write.
6. Run a source-resolution preflight only. It binds command, exit, stdout,
   stderr, Node/V8, absolute runtime/package paths, bytes, hashes, version,
   resolved imported file, environment, and compile-cache state. It performs
   no scientific probe or timing.
7. Only if that preflight is GREEN may the unchanged N-IETM scientific probe
   begin. Ohm/Dubroy/GPeg equivalence and the eleven existing fatal gates run
   before timing. The first RED freezes N2a permanently; no same-root patch,
   retry, clearing, or reseal is allowed.

The previous prior-art ruling, callback/environment/source-read/diagnostic/
effect/complete-product integration hypothesis, PL-BE-only eligibility,
equal M2 and semantic-envelope products, exact UTF-16 and EOF laws, full
artifact/raw authority, and first-RED stop remain unchanged. A descriptive
local 2x or 3x result cannot enter PL-2X or PL-3X. Product source, Value-owned
CSS implementation, package/release work, and downstream execution remain
closed.

## Dispatch boundary

Disposition: `DISPATCH_AUTHORIZED_NOT_SENT`.

The root coordinator may send one correction packet only after this ruling is
committed, its document SHA-256 is independently reproduced, the failed-root
and residue census still matches, the N2a path is still absent, and both Ohm
pins pass the pre-root checks above. The packet's first executable action is
input/source-resolution preflight; it is not root repair and not scientific
timing. This owner turn sends no task.
