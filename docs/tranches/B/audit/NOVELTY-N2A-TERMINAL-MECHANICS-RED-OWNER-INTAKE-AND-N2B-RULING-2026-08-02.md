# Novelty N2a terminal mechanics-RED owner intake and N2b ruling

Date: 2026-08-02

Status: **N2A TERMINAL MECHANICS RED — ZERO CREDIT — N2B SAME-TASK FRESH-ROOT DISPATCH AUTHORIZED, NOT SENT**

## Scope and immutable predecessors

This tranche-development ruling freezes the split N2a transaction, assigns it
zero credit, amends the narrow integration hypothesis from current source
archaeology, and defines the sole legal N2b correction. It changes no parser
source, public API, Value-owned CSS source, package, candidate, release, or
consumer coordinate.

- repository: `/Users/mkbabb/Programming/parse-that-css-totality`;
- branch: `codex/css-totality-combinators-20260729`;
- predecessor HEAD:
  `9f62aeb2499734e8503e14283e63f2e84c6004b3`;
- N1-A4 authority:
  `81b2a0aacebc126ac1a093407cb2e24ed5d78ee8`;
- N2-F0 ruling SHA-256:
  `7c3cda9b8011c5efec3c1c3b2fa33342698a4a8fa9acf2524f75fbdbc8f64071`;
- sole idle Luna task:
  `019fb12c-2624-7732-909a-e1e87ce7ef24`;
- authorized N2a writer root:
  `/Users/mkbabb/Documents/Codex/2026-08-01/parser-novelty-n2a-ietm-f0`;
- misdirected N2a locus:
  `/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2a-ietm-f0`;
- proposed N2b writer root:
  `/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2b-ietm-f0`.

The proposed N2b root was absent at owner census. This turn neither creates it
nor sends a task.

## N2a failure ruling

The first and only attempted preflight command bound `TMPDIR` and stdout/stderr
destinations to the Aug-2 N2a locus while the only source file and its literal
`ROOT` belonged to the Aug-1 writer root. The Aug-2 `raw/` directory did not
exist, so shell redirection failed before Node invocation. The shell wrapper
also attempted to use zsh's read-only `status` name. There is no Node output,
source-resolution result, scientific probe, timing, prior-art disposition, or
KEEP/FOLD result.

Recorded attempted command, with the supplied shortened destinations expanded
to the two literal Aug-2 raw paths:

```text
env -i PATH=/opt/homebrew/bin:/usr/bin:/bin TMPDIR=/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2a-ietm-f0/tmp NODE_DISABLE_COMPILE_CACHE=1 NODE_OPTIONS= /opt/homebrew/Cellar/node/26.0.0/bin/node source/source-resolution-preflight.mjs > /Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2a-ietm-f0/raw/preflight.stdout.ndjson 2> /Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2a-ietm-f0/raw/preflight.stderr.txt
```

The current source confirms the split:

- `ROOT`, `TMP`, `RAW`, and `RESULTS` are literal Aug-1 paths;
- it requires `ROOT/root-birth.json`, which is absent;
- the only `root-birth.json` is under the Aug-2 locus;
- that JSON names the Aug-2 locus but embeds the Aug-1 root's initial
  `00:04:20`, `size=160`, `nlink=5` metadata;
- the observed Aug-2 root was instead born at `00:04:26`, is `size=96`, and
  has `nlink=3`.

The transaction therefore has no coherent writer-root identity. N2a is
terminal mechanics evidence, not an executable or scientific packet.

## Reproduced frozen census

### Authorized Aug-1 writer root

Observed root stat:

```text
size=192 mode=drwxr-xr-x nlink=6
birth=2026-08-02T00:04:20-0400
ctime=2026-08-02T00:05:26-0400
mtime=2026-08-02T00:05:25-0400
```

It contains exactly four child directories (`raw`, `results`, `source`,
`tmp`), five directories including the root, one regular file, no symlinks,
and no special nodes. `raw/`, `results/`, and `tmp/` are empty.
`root-birth.json` is absent.

| Path | Bytes | Mode / links | SHA-256 | Birth / change / modify |
|---|---:|---|---|---|
| `source/source-resolution-preflight.mjs` | 6,405 | `0644` / `1` | `e617e5788ccf58c0e97121ccf4b43321a0e65a64568963235513be7d42b623b3` | `00:05:19` / `00:05:26` / `00:05:19` |

### Misdirected Aug-2 locus

Observed root stat:

```text
size=96 mode=drwxr-xr-x nlink=3
birth=2026-08-02T00:04:26-0400
ctime=2026-08-02T00:05:26-0400
mtime=2026-08-02T00:05:22-0400
```

It contains exactly the root directory and one regular file, with no
`raw/`, `results/`, `source/`, or `tmp/` directory and no symlink.

| Path | Bytes | Mode / links | SHA-256 | Birth / change / modify |
|---|---:|---|---|---|
| `root-birth.json` | 464 | `0644` / `1` | `274c21e98b2fea0421be23264772f8ae723e635ef73811d05e33fd26a1920c9b` | `00:04:26` / `00:05:25` / `00:04:26` |

Times in both tables are on 2026-08-02 at UTC-04:00. Both loci and all their
bytes are frozen. They must not be repaired, merged, copied into a successor,
cleaned, rerun, or resealed.

## Credit disposition

| Plane | N2a disposition |
|---|---|
| writer-root identity | `RED: split Aug-1/Aug-2 transaction` |
| source-resolution preflight | `NOT INVOKED / RED` |
| root-birth authority | `RED: absent at writer root; misbound elsewhere` |
| scientific prior-art/fatal gates | `NOT RUN / NO CREDIT` |
| callback/effect/complete-product integration | `NOT RUN / NO CREDIT` |
| timing, allocation, GC, deopt, IC, package economics | `NOT RUN / NO CREDIT` |
| PL-BE, PL-2X, PL-3X, historical 10x | `0` |
| parser runtime, CSS, Value, Keyframes, BBNF, candidate, API, package, release | `0` |

N2-F0 and N2a are two terminal mechanics attempts. Scientific N2 receipts
remain `0/4`; N3 and every law family remain unchanged.

## N2b input pins

N2b must use these absolute regular-file inputs directly. Package-name,
working-directory, `NODE_PATH`, and unpinned `node_modules` resolution remain
forbidden.

| Input | Bytes | Mode / links | SHA-256 | Contract |
|---|---:|---|---|---|
| `/Users/mkbabb/Programming/parse-that-css-totality/typescript/node_modules/ohm-js/dist/ohm.cjs` | 192,483 | `0644` / `1` | `8c8c6544103f6e9b5361f3d462f1929f2524993d2c0dc1464fa08dc5ce3e5b34` | absolute Ohm runtime |
| `/Users/mkbabb/Programming/parse-that-css-totality/typescript/node_modules/ohm-js/package.json` | 5,235 | `0644` / `1` | `9cff75d790c89c4a1d8eab1ae6a555cb491281d4d3307fc811294cda94d03350` | exact version `17.5.0` |

Owner replay under Node `v26.0.0` / V8 `14.6.202.33-node.19` loaded callable
`grammar` from the absolute runtime and reproduced version `17.5.0`. This is a
read-only pin check, not N2b credit.

## Narrow N-IETM integration amendment

Broad N-IETM novelty remains `REJECT/FOLD PRIOR ART`. The only born-RED local
hypothesis is whether parse-that can reuse recognition state safely across
edits while always reconstructing the complete public product.

`ParserContext` is audit prose only. It is public, mutable, incomplete, and
forgeable; neither its presence nor its fields authorize reuse. The private
prototype may trust only library-constructed parser nodes certified in an
unexported `WeakMap<Parser, NodeProof>` whose entries cannot be supplied,
mutated, or forged by a consumer.

Initial reuse eligibility is limited to callback-free recognition islands:
`string`, `eof`, and closed certified structural compositions of eligible
children. The following remain opaque and rerun:

- arbitrary `new Parser` and custom parser functions;
- `map`, `mapState`, `chain`, `call`, `debug`, and `lazy`;
- custom RegExp and effectful recovery;
- any semantic/effect callback or node lacking a complete proof; and
- RegExp generally, whose examined dependency reaches EOF unless a separately
  proved bounded subset supplies an exact read frontier.

Semantic and effect callbacks always rerun, and reconstructed values must be
fresh. Only recognition, offset, and selected branch may be reused, and only
when complete source/read/lookahead, graph/action/environment, diagnostic
policy, and effect dependencies prove the reuse. Left-recursion seed, head,
in-progress growth, and other transient epoch state never persist.

Every edit remains equal to fresh accepted/rebuilt M2 plus the semantic-law
envelope for values, spans, slots, terminal/frontier/mismatch, diagnostics,
fault, rollback, recovery, provenance, depth, branching isolation, arbitrary
source reads, environment identity, and effect-exactly-once. Economics measure
dynamic work actually skipped, tracing/index/relocation cost, retained memory,
and end-to-end K-edit break-even; static certified-node count is not a benefit.
The family is PL-BE-only and born RED.

## N-DNF-BIR prior-art boundary

Scanner/combinator fusion, staged direct code, lossless CST construction, and
bidirectional parse/print are established prior art. `N-DNF-BIR` therefore has
no world-novelty claim. It may remain only as a second born-RED parse-that
integration route.

Before any implementation or timing it must prove exact non-isomorphism to the
retired S, C, V, and E load-bearing mechanisms. It is immediately folded or
pruned if it uses a token/index/tape, a scanner or lexer stage, a generic VM or
closure fallback, or a CST/event/region followed by a separate typed-product
projection. The route must directly preserve scannerless source semantics,
complete products, recovery, callbacks, package/build economics, and the
Value-owned grammar boundary. No N2b authority is granted to this route.

## N2b same-task fresh-root law

Exactly one bounded N2b correction is authorized but not dispatched. It may
reuse only the idle task `019fb12c-2624-7732-909a-e1e87ce7ef24`; no new task,
owner lineage, sibling executor, duplicate root, or N-DNF-BIR implementation
is authorized.

The order is binding:

1. Re-census both N2a loci and reproduce every stat, count, and SHA above.
   Any drift stops.
2. Verify the N2b writer root is absent and both Ohm pins are exact.
3. Materialize only the literal writer root
   `/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2b-ietm-f0`.
   No clock/date substitution, relative root, environment-derived root, or
   shell variable may choose it.
4. Record `root-birth.json` inside that literal root before source writes, then
   create exactly `raw/`, `results/`, `source/`, and `tmp/`. The birth record,
   source constant, process working directory, `TMPDIR`, result descriptors,
   and every output must equal the same literal root byte-for-byte.
5. Use a Node-owned preflight transaction with a literal `WRITER_ROOT`. Do not
   assign shell variables, use reserved zsh names, derive dates, or use shell
   stdout/stderr redirection. The source writes its own canonical result and
   exit evidence under the writer root. Any exception before a complete result
   is RED and freezes the root.
6. Invoke the preflight with the absolute Node binary and absolute source path,
   an empty explicit environment, the root-contained literal `TMPDIR`, compile
   cache disabled, and the execution tool's `workdir` set directly to the N2b
   root. It loads only the absolute Ohm inputs above.
7. Source-resolution preflight remains the first executable gate. It binds
   command, exit, Node/V8, environment, compile-cache state, writer-root birth,
   exact input files, and a pre/post root inventory. It starts no scientific
   probe and no timing.
8. Only after a GREEN preflight may the amended N-IETM fatal/equality probe
   run. The existing Ohm/Dubroy/GPeg equivalence and first-RED laws remain.
   The first RED freezes N2b permanently; no same-root patch, retry, clearing,
   repair, or reseal is permitted.

Every temporary, raw, result, log, stderr, receipt, and checksum byte must be
inside N2b. `/tmp`, `os.tmpdir()` fallback, the two N2a loci, N2-F0, and all
other date roots are read-only. Product source, public API, Value CSS, package,
candidate, release, and downstream execution remain closed.

The preflight invocation is literal and has no shell assignment or
redirection. Its source owns root-contained result/exit evidence:

```text
env -i PATH=/opt/homebrew/bin:/usr/bin:/bin TMPDIR=/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2b-ietm-f0/tmp NODE_DISABLE_COMPILE_CACHE=1 NODE_OPTIONS= /opt/homebrew/Cellar/node/26.0.0/bin/node /Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2b-ietm-f0/source/source-resolution-preflight.mjs
```

The execution tool, not a shell `cd`, supplies workdir
`/Users/mkbabb/Documents/Codex/2026-08-02/parser-novelty-n2b-ietm-f0`.

## Dispatch boundary

Disposition: `DISPATCH_AUTHORIZED_NOT_SENT`.

The root coordinator may send one N2b packet only after this ruling is
committed, its SHA-256 is independently reproduced, both N2a censuses still
match, the N2b path remains absent, and both Ohm pins pass. The packet must
name the same Luna task, literal N2b root, amended WeakMap/recognition-island
law, and first source-resolution action. This owner turn sends no task.
