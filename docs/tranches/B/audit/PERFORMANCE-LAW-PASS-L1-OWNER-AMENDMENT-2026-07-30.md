# Performance-law Pass L1 owner amendment

Date: 2026-07-30

Status: **AMEND — L1 OPEN — ORIGINAL LUNA PACKET IMMUTABLE — ZERO
DOWNSTREAM CREDIT**

## Ruling

Pass L1 research and common Sol synthesis are accepted as formation research.
The first Luna neutral harness packet is not accepted as
`HARNESS-MECHANICS-VALIDATED`. Its raw coordinate arithmetic, emitted
digest/sink/counter equality, and exact bootstrap replay are sound, but its
semantic corpus, product sentinels, generic arm boundary, schemas, and cost
labels contain false greens.

The packet remains immutable negative evidence. It is not regenerated,
relabeled, or deleted. One correction-only Luna packet and one same-scope
fresh-Sol replay are required before owner agglomeration may close L1.

No law, threshold, parser mechanism, runtime, API, candidate, CSS surface,
consumer, package, release, or rebind is selected or admitted.

## Bound evidence

| Evidence | Coordinate |
|---|---|
| Parser law authority | commit `ef20a989cec62e68e89a77d44dc6865a663431ff`; SHA-256 `f58442ede34e190d7260385001abd2e958c1aaea39bc20a4ed1939939a8bf9c2` |
| PL-3X Sol research | manifest `74b849e4e3d05ccbac44c6438fc9ccf18234eb4369a5ced14f3baad2044dd887` |
| PL-2X Sol research | manifest `566384ae1c857c3df176362ac72647bd7bd8992b356e2f1d906b11f2ec26a8ef` |
| PL-BE Sol research | manifest `464710e38acce7022c86de6f1a55570d6bc3be32cf7413219831254093494e1f` |
| Common Sol synthesis | manifest `06de222cb5202d9e2da167c170dae4d18b2cebc166f808f28983123fd6101a84` |
| Original Luna packet | manifest `980d7781c2912c2501247978b3153d7f1fee486b4bff51820b55c5fdeb221e27` |
| Original Luna raw | SHA-256 `565148af55f71ce2973094f73149808371aed2f7916bd04c3a2919fbd803c438`; 585,386,359 bytes; 2,757 lines |
| Fresh Sol critique | manifest `3cd94feeaa1550d3294fce55cd9754ff0d97e34ec962f96f4afa1283243c8da4` |
| Fresh Sol report | SHA-256 `4763104953bb60b0eed61c0698b8d901631f192e394f21e9fbb9127806c737e3` |
| Fresh Sol findings | SHA-256 `8fefc2549401a19acf96f20eca9a19551de25b4c598c6a02cce33e9010a50ed5` |
| Fresh Sol replay | SHA-256 `08d6b68717d49a8a8c35ba305ad05bab9faf78d4ec870d310495499c87f2ebf0` |
| Fresh Sol mutants | SHA-256 `a29c4ba1e7753114d08e5c0bf1a706740a103ab31ff531f3b26d0b6b6a5049d4` |

The owner independently verified the fresh-Sol topology as six regular files,
zero subdirectories, and two green 5/5 manifest passes.

## Preserved neutral facts

The following facts are valid for the original Luna packet and nothing more:

- exact accepted M2
  `de36d57dccdd20068b8c11a78f6e83d42e7d681f`, accepted bundle
  `3751609e01dc46f5ad50f31d4d5f9ecc05e2aad5ccdb61e1508ae780715a8770`,
  and rebuilt neutral bundle
  `ea37da11c962d8be19945c3537948b6a7e01879f2f922267ee31e9f224ec70ab`;
- 1 identity, 5 mutant, 2,170 hot, 476 cold, and 105 instrumentation
  receipts, with 496 unique processes;
- 2,170/2,170 unique hot coordinates, balanced 1,085/1,085 AB/BA order,
  and 119/119 internally equal four-receipt cold groups;
- 35 null-counter and 84 non-null-counter cold groups;
- equal, nonzero accepted/rebuilt aggregate counts for all eleven named
  primitives;
- 48 reproduced row ratios; and
- 192 independently replayed bootstrap distributions,
  158,120,256 tuple statistics, zero digest/endpoint/statistic mismatches,
  with PL-3X median and PL-2X geometric mean kept separate.

These facts prove neutral scheduling and arithmetic mechanics only. They do
not validate the advertised semantic rows or select a performance law.

## Fatal findings

1. Four product-omission mutants mutate an unused clone and then assign their
   passing terminal directly. Removing all four deletes still produces the
   same terminal. No general schema/equality/freeze sentinel was proven.
2. Generic nested and delimited rows use one handwritten `createRuntime`
   implementation for both arms and ignore the accepted/rebuilt bundles'
   generic exports. They are self/self, not arm-distinct M2/rebuilt-M2.
3. `json-composed` routing is unreachable because the live-JSON suite return
   precedes the row-prefix branch. Rollback, recovery, diagnostics, spans, and
   composed provenance are absent.
4. Named semantic planes can be labels without contents. The delimited
   typed-fault result has `fault:null` and `isError:false`; both named generic
   rollback rows have `rollback:null`; one direct JSON mismatch is a
   non-error partial parse labeled by the row.
5. All twelve nested/delimited alternating rows have one fixture. The two
   authored alternative-success fixtures are unused.
6. Nested zero-width-at-nonzero-cursor and recovery-nonprogress fixtures are
   absent. Delimited escape and bad-URL fixtures are absent. Blanket all-true
   feature maps do not prove reachability.
7. Declared hot schemas differ from emitted records; shared vectors,
   warmups, and aggregate sinks are not sealed; measured hot/cold lanes do not
   perform deep equality and freeze validation over their in-memory products.
8. Allocation and retained-heap labels describe endpoint deltas and
   serialized length, not the named costs. GC/IC/deopt fields are absent or
   null, trace bytes are discarded, every cold arm imports both bundles, and
   exact rebuild provenance is incomplete.

## Correction-only Luna transaction

Resume the existing Luna xhigh task
`019fb12c-2624-7732-909a-e1e87ce7ef24`. Create no task and no child. The sole
write root is:

```text
/Users/mkbabb/Documents/Codex/2026-07-30/parser-law-pass1-common-luna-prototype-a1/outputs/**
```

All repositories, original packets, caches, products, and package state are
read-only. The root must be absent before the transaction. The correction:

1. uses each accepted/rebuilt bundle's exported generic `Parser`, leaf,
   dispatch, sequencing, mapping, repetition, lazy, recovery, and EOF
   mechanisms through one thin counter adapter; it defines no second Parser
   or executor;
2. routes JSON composition before direct live JSON and asserts non-null,
   substantive rollback/recovery/diagnostic/span/provenance contents;
3. derives status from actual result state, then rejects any row whose named
   plane invariant is false before timing;
4. provides at least two content-distinct fixtures for every alternating
   nested/delimited row, proves both are selected, and adds reachable
   zero-width-at-nonzero-cursor, recovery-nonprogress, escape, and bad-URL
   fixtures with construct-specific feature maps;
5. routes the selection mutation and four actual mutated products through one
   general vector/schema/deep-equality/freeze sentinel. Diagnostics and first
   paths are computed by the sentinel, never assigned per mutant;
6. freezes the shared vector; deep-compares and freeze-checks every in-memory
   hot/cold product before discarding it; seals warmup and aggregate
   equality/sink receipts; and makes schemas describe emitted records exactly;
7. imports only the assayed arm in each cold worker and seals the accepted and
   rebuilt entry/source lists, command, toolchain, and expected hashes;
8. renames endpoint heap and canonical-length proxies honestly. It retains
   exact runtime-health bytes where collected and leaves unavailable
   allocation/GC/IC/deopt/package fields explicitly `null/BLOCKED`; it does
   not claim cost completeness;
9. preserves identical fixture vectors, balanced scheduling, complete frozen
   products, family-specific bootstrap statistics, scannerless source-direct
   execution, and zero product/full-CSS credit; and
10. writes a final manifest last, verifies it twice, and writes nothing after
    sealing.

The output topology is exactly fourteen regular files and zero
subdirectories/special entries:

```text
runner.mjs
bootstrap.mjs
accepted-m2.bundle.mjs
rebuilt-m2.bundle.mjs
fixtures.json
schemas.json
raw.ndjson
results.json
mutants.json
runtime-health.ndjson
rebuild.json
RECEIPT.md
INTEGRITY.md
MANIFEST.sha256
```

`MANIFEST.sha256` lists the preceding thirteen files. `rebuild.json` owns the
entry/source/command/toolchain and both expected bundle hashes.
`runtime-health.ndjson` owns exact retained child output plus parsed
GC/deopt/code-state observations; unavailable IC/allocation fields remain
named `null/BLOCKED`.

The corrected roster is exact:

- 35 hot rows: the prior 31 row identities plus fixed/alternating nested
  zero-width-at-nonzero-cursor and recovery-nonprogress-fault rows;
- 21 cold assay rows: nineteen fixed rows plus the two new alternating
  zero-width and recovery-nonprogress rows;
- seven hot processes, ten 2,000-selection paired batches per row, and one
  64-selection warmup per arm;
- one sealed hot-process proof per `(row, process)` containing the warmup and
  aggregate vector/product/freeze/sink/counter equality receipt;
- 588 arm-specific cold processes, seven four-receipt blocks per cold row;
- seven instrumentation processes over the same three success rows;
- one identity plus five executable mutant cells.

The resulting raw topology is 3,394 lines: 1 identity, 5 mutants, 2,450 hot
paired batches, 245 hot-process proofs, 588 cold receipts, and 105
instrumentation receipts. Measured hot work is 9,800,000 arm invocations;
warmup is 31,360; cold is 588; instrumentation parser work is 5,376;
identity/mutants are 768; total parser arm invocations are 9,838,092, plus
2,688 no-parse instrumentation operations across 608 fresh processes. Any
count drift is terminal before the manifest.

Pre-seal owner inspection adds six binding implementation corrections without
changing that topology:

1. cold timing begins before the arm-specific import and separately records
   import/evaluation and grammar-construction intervals;
2. every cold receipt preserves its row's actual `shapeMode`;
3. hot process summaries retain `processOrdinal` before ordering/bootstrap;
4. bundle export count is not mislabeled module count; module count is exact
   or `null/BLOCKED`;
5. rebuild provenance binds the entry and every transitive esbuild input, or
   remains explicitly `BLOCKED`;
6. recovery state and diagnostics change only when exported `recover`
   actually selects its recovery value. Nominal success proves no recovery,
   fault, rollback, or diagnostic leakage.

Any raw produced before these corrections is unsealed and inadmissible. The
same A1 root may be rebuilt and rerun before `MANIFEST.sha256`; no sealed input
or earlier packet may be overwritten.

The A1 packet may accept corrected L1 neutral mechanics while leaving cost
completeness RED. It may not select PL-3X, PL-2X, PL-BE, a parser mechanism,
or a threshold. The original raw bytes remain the only evidence for their
original transaction and are never copied forward as corrected timings.

After A1 seals, the same fresh non-author Sol scope replays it. Any unequal
vector/product/freeze/sink, label-only plane, hardcoded sentinel, shared
executor, schema drift, or false cost label is `AMEND` or `REJECT`; no ratio
or bootstrap survives the first such defect.

## Coordination bound at this agglomeration

- Latest Value receipt-only coordinate independently reproduces from the
  isolated Value worktree as matrix
  `5167f163c19cc7eaf054e6c98df15f3c1a0de4510c5ba283946acff6bbb3f8fa`
  and status
  `90e895dd246ebf454f5dc257f6fe3e53cbfeebb3fdeff3dcf4ea342fe06c2076`.
  Value remains `14/34`; all three law families remain `0/5`.
- The supplied Keyframes external receipt reports formation reconverged with
  zero execution: FINAL
  `864565973972120780c1343e6688ea2a87320a610da77471afa418a4ecc8f7f6`,
  corpus `f182b2009231a558cad8b453b548ba2be03c847200fcf22a09feb2c412c6cfc6`,
  replacement Clean A
  `acf0edf3f75430270875695cd6f416431980e665e984aa9a6d2c57c2fe500c31`,
  and Clean B
  `76d14724130ae41399b4452d6bebf3fc659aba649356b3a3499bdd91f13fae6a`.
  No matching object was found in the visible `9167` Keyframes checkout or
  local evidence roots, whose current FINAL/Clean hashes differ. The receipt
  is therefore unverified dependency chronology, not parser readiness proof.
- The supplied Fourier terminal parser dependency is one-way and must not be
  returned to Value: parser MD
  `9b1dde9442c0ca65a9f5e5c47eeaf4a78e878400b0afb91575a1440a1417200b`,
  parser JSON
  `16a5bf1879b50cf4d2cd6f596f4cead4f832067544da2a948b59fa372d33dd3f`,
  N.md `a66f3e44a82793cb83c1373a57cb5dc34e5594009a12b5d0683c1e48fdd1858b`,
  handoff
  `0a1a41e6ad031a3ee7744da332933c5fa87079249dd48a3620d8e3971b19c517`,
  and precepts audit
  `aa533e69683075859bf75189a28ce1db3319f095d29483adb9aceca33014bef4`.

Only the Value pair is independently reproduced here. The Keyframes and
Fourier coordinates remain supplied dependency chronology. None grants
parser-law, CSS, product, execution, release, or rebind credit.

## Convergence

Round Zero is complete. L1 family research and common Sol synthesis are
complete. The first Luna/fresh-Sol loop is terminal `AMEND`; owner
agglomeration is complete only as an amendment boundary. Pass L1 remains
open. L2, L3, Formation Clean A/B, law selection, and formation admission are
blocked.
