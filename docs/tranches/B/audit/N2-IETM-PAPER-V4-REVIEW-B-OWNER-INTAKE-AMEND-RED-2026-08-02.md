# N2 IETM paper v4 — Review B owner intake

Date: 2026-08-02

Status: **TERMINAL AMEND/RED — PAPER ONLY — ZERO CREDIT — V4 FROZEN**

## Frozen input

- packet commit: `483dcb1bbfb6f949459597b5105a95a62432edb1`;
- packet tree: `fa240134177478e4636b018ae3b2c73329cdd21d`;
- packet root: `docs/tranches/B/research/N2-IETM-PAPER-v4/`;
- manifest SHA-256:
  `fa804b2976f5d7fbf9911ee21450fc6ae3b6e05cf1cd8528d7ec8473848db53f`;
- manifest replay: `9/9 EXACT`;
- topology: `10 regular files / 36,791 bytes`;
- Review A intake:
  `docs/tranches/B/audit/N2-IETM-PAPER-V4-REVIEW-A-OWNER-INTAKE-AMEND-RED-2026-08-02.md`;
- parser, source, prototype, Node, AST, test, build, benchmark, CSS, product,
  package, execution, candidate, release, and rebind credit: `0`.

This intake records the supplied independent Review B ruling without claiming
an external review-file identity that was not provided. V4 remains immutable;
N3 remains an independent paper family.

## First exact falsifier

```text
P5_BUNDLE_RUN_PRODUCTION_LEAF_UNBOUND
```

V4 states that P5 owns a complete bundle/run validator but its exact nineteen
P6 controls name only one P5 leaf, `validateFreshControlRow`. No production
leaf, unique code, concrete target, or owner/non-owner control is bound for:

- candidate versus control role;
- closed bundle membership and root;
- external input authority;
- actual executable and harness bytes;
- command capture and candidate-before-control chronology;
- capability isolation and freshness;
- action/callback/environment observation; or
- actual post-run seal construction.

The claimed aggregate validator can therefore remain a shadow group, while
the machine control denominator never exercises its load-bearing predicates.

## Independent secondary findings

1. E07 receives detached synthetic raw bytes rather than an authenticated
   bundle, role, row, blob descriptor, external pin, and cloned actual bytes.
2. P6 injectors do not bind exact concrete target, authenticated before/after
   bytes or values, injector operation, disabled owner leaf, or every
   non-owner retention.
3. Audit-wrapper/predicate function identity is not bound, so a shadow bypass
   registry can replace the production predicate.
4. The v4 machine manifest records only counts; it does not map each control ID
   to profile, target, before/after, owner leaf, unique code, bypass, retention,
   erasure, and control-of-control receipt.
5. Candidate freshness and isolation are prose-only and omit exact filesystem,
   process, network, file-descriptor, and capability observations.

## Preserved structure

- E01–E25 structure/acyclicity, Coordinate graph closure, typed epochs, and
  P7's honest executable-AST budget RED remain KEEP;
- v4's `420/240` arithmetic is correct for its incomplete `19/14` registries;
  a successor must derive updated denominators from its complete machine rows;
- all retained structure remains paper evidence with zero execution credit.

## Root-routed correction boundary

The root ruling authorizes only a fresh paper v5. V5 must bind exact P5
production leaves and unique codes, authenticated actual-byte E07/E08 domains,
a source-owned predicate identity, a full machine control registry, independent
candidate/control pins and observation seals, and fail-closed freshness and
capability observations. Any missing row stays RED rather than being hidden in
an aggregate leaf.

This intake grants no v5 correctness/review credit and no A5/B5 dispatch.

## Exact boundary

- Review B result: `TERMINAL AMEND/RED`;
- v4: `FROZEN / UNCHANGED`;
- A4/B4 or retrospective review credit: `0`;
- scientific/equivalence/performance/novelty/CSS/product/law/release credit:
  `0`;
- authorized continuation: `PAPER V5 ONLY` under the separate root ruling;
- N2e/source/execution/review dispatch: `WITHHELD / NONE`.
