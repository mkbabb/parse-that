# N2 IETM paper v3 — Review A3 owner intake

Date: 2026-08-02

Status: **TERMINAL AMEND/RED — PAPER ONLY — ZERO CREDIT — V4/A4 NOT AUTHORIZED**

## Frozen input

- packet commit: `1fae739bb251541a37df17dd4a3dc54b793b5835`;
- packet tree: `6f5c9106c5e163dac6fe5e412dfff6c5e98b1c47`;
- packet root: `docs/tranches/B/research/N2-IETM-PAPER-v3/`;
- manifest SHA-256:
  `b79b03030a38ff1724d6f20c25fef7ffbb1820246e840ea085b63ed73905e322`;
- manifest replay: `9/9 EXACT`;
- packet diff from commit: `EMPTY`;
- topology: `10 regular files / 31,179 bytes`;
- source, prototype, Node, parser, AST, test, build, benchmark, CSS, product,
  package, review dispatch, and N2e execution: `0`.

The v3 packet is immutable. This intake records the supplied fresh Review A3
ruling without claiming an external review-file identity that was not
provided. It grants no correction, source, execution, or v4 authority.

## First exact falsifier

```text
P6_PRODUCTION_LEAF_GRAPH_DISCONNECTED
```

P6 claims nineteen production leaves over declaration, ledger, edit, effect,
depth, product, fresh-control, and codec invariants owned across P0–P5. Its
complete edge table, however, contains only P1→P6 decode/encode and P6→P7
claim edges, followed by “No other edge exists.” P0, P2, P3, P4, and P5 expose
no typed value or predicate edge to any P6 leaf.

The non-codec leaves therefore cannot be production-reached under the declared
graph. They are shadow/unreachable leaves, or they require hidden P0–P5→P6
dependencies. In either case the claimed owner suppression and all-non-owner
retention proofs cannot exercise the same production predicate. V3 is RED
before source or execution.

## Independent secondary findings

1. Effect-epoch selection is ambiguous: multiple epochs can repeat event keys,
   while E04, `ObservedEvent`, and P4 lookup bind no authenticated selected
   `identityEpochId`.
2. `CandidateReceiptBytes` are externally supplied but have no owner pin,
   closed byte schema, raw command, executor, or production-consumed authority.
3. P5 lists raw blobs but does not close the top-level `RawControlBundle`
   membership, unique-use inventory, canonical root, or external root pin.
4. P6 and P7 state per-mutant controls but do not state exact total outcome
   denominators for baseline, owner, non-owner, erasure, unknown, and duplicate
   rows.

## Independently preserved GREEN structure

- P2→P3 one-way depth relocation is structurally clean;
- E01–E18 occur exactly twice with byte-identical owner rows;
- P0–P7 contain `395` nonblank lines and every interface is below `100`;
- future module ceilings sum exactly `850`;
- none of these structural facts grants scientific or execution credit.

## Non-authorizing future recommendation

A later owner/root ruling may choose between typed production-leaf edges into
P6 and reassignment of each invariant to its P0–P5 production owner, with
module budgets recomputed. The same future paper would need authenticated
effect-epoch selection, owner-bound candidate execution/raw command/schema,
coordinate-bearing semantic graphs, closed P5 membership/root, exact hostile
collateral/denominators, and P7 structural own-reason ordering. This paragraph
is a recommendation only: no v4 name, packet, source, review, or execution is
authorized.

## Exact boundary

- Review A3 terminal result: `AMEND/RED`;
- scientific/equivalence/performance/novelty/CSS/product/law/release credit:
  `0`;
- v3 mutation or reseal: `FORBIDDEN`;
- v4, N2e, source, execution, and Review A4: `WITHHELD / NOT AUTHORIZED`;
- next action: bank Review B3 independently, update external tranche status,
  and await a later owner/root ruling.
