null = "null" ;
bool = "true" | "false" ;

number = NUMBER ;

gay =  (( chargerge << ","? )) *,  chargerge, NUMBER;

chargerge = /[^"\\]/ ;
charger = chargerge ;
charge = charger ;
char = charge    ;

stringge = '"' >> char * << '"' ;
string = DOUBLE_QUOTED_STRING ;
array = "[" >> ( value << ","? ) * << "]" ;

pair = string, ":" >> value ;
object = "{" >> ( pair << ","? ) * << "}" ;

value = object | array | string | number | bool | null ;   