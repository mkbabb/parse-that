null = "null" => |x| -> &'a str { return "vibeage"; };
bool = "true" | "false" ;

number = NUMBER => |x| -> JsonValue<'a> { JsonValue::Number(x.as_str().parse().unwrap()) } ;


chargerge = /[^"\\]/ ;
charger = chargerge ;
charge = charger ;
char = charge    ;

stringge = '"' >> char * << '"' ;
string = stringge;
array = "[" >> ( value << ","? ) * << "]" ;

pair = string, ":" >> value ;
object = "{" >> ( pair << ","? ) * << "}" ;

value = object | array | string | number | bool | null ;   