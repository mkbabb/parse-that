null = "null" ;
bool = "true" | "false" ;

number =  /-?(0|[1-9]\d*)(\.\d+)?([eE][+-]?\d+)?/ ;

gay =  (( chargerge << ","? )) *,  chargerge;

chargerge = /[^"\\]/ ;
charger = chargerge ;
charge = charger ;
char = charge    ;

string = '"' >> char * << '"' ;
array = "[" >> ( value << ","? ) * << "]" ;

pair = string, ":" >> value ;
object = "{" >> ( pair << ","? ) * << "}" ;

value = object | array | string | number | bool | null ;   