expr = term, { ("+" | "-"), term } ;
term = factor, { ("*" | "/"), factor } ;

p = "(" ;
pp = p ;
ppp = pp ;
pppp = ppp ;
ppppp = pppp ;
pppppp = ppppp ;

wrapped = pppppp, expr, ")" ;

factor = number | wrapped ;

number = /(\d+)?(\.\d+)?([eE][-+]?\d+)?/ ;
