SERVED MODEL: claude-opus-5[1m]

X.P.W4.g2 — ESC-W4g-1 (COHESION §0ah bullet 4 · W4.md SIXTH ADDENDUM 2026-09-20)
THE CARRIED LIST: measured acceptance, 2026-09-20. NEW dated file, beside (E-3).

Artefact under cure:
  <p2>/typescript/test/css-equivalence/emit-divergence-ledger.mjs
Canonical ledger (READ ONLY — byte-frozen, E-3, NOT regenerated by this unit):
  /Users/mkbabb/Programming/value.js/docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md
Pin used at every emission below:
  --pinned-value-commit 6aca86020b6b2605e7d0f04fccb6601746e387f7
Scratch --out root (no repo byte written by any emission):
  <scratch>/g2/

================================================================================
§1  BORN-RED, measured at the artefact BEFORE the cure
================================================================================

⟨grep -c '## §10' test/css-equivalence/emit-divergence-ledger.mjs⟩ → 0

The carry at :171 was a SINGLE block:
    const carriedAt = previous.indexOf("\n### §6.");
    ... sliced to the next "\n## " / "\n---\n"
so the two dated sections HAND-APPENDED to the canonical ledger after F-e7's cure —
`## §10 — rulingId appends, X.P.W4.f` (canonical line 1084) and
`## §11 — the NEW divergence X.P.W4.h opens` (canonical line 1135) — were outside every
carried region.

⟨node test/css-equivalence/emit-divergence-ledger.mjs --out <scratch>/before-1.md \
       --pinned-value-commit 6aca8602…⟩ →
  wrote …/before-1.md — 131539 B · 1080 lines · 46 rows · empty directions 0 ·
  `.e`'s §6 block carried 47 lines
  EXIT=0

Over that emission:
  ⟨grep -c '^## §10' before-1.md⟩            → 0
  ⟨grep -c '^## §11' before-1.md⟩            → 0
  ⟨grep -c 'RETIRED' before-1.md⟩            → 0

BORN-RED CONFIRMED AT THE BYTES, not supposed: a re-emission dropped both sections and
every retirement row §10.2 carries. This is the negative control for §3 below — the same
command, the same pin, the same scratch path, one commit apart.

================================================================================
§2  THE CURE, as landed (the shape, not a paraphrase)
================================================================================

    const CARRIED_REGIONS = [
        { id: "§6",  mark: "\n### §6.", toTail: false },
        { id: "§10", mark: "\n## §10",  toTail: true  },
    ];
    const sliceCarry = ({ mark, toTail }) => {
        const at = previous.indexOf(mark);
        if (at < 0) return [];
        const rest = previous.slice(at + 1);
        if (toTail) return rest.replace(/\s+$/, "").split("\n");
        const ends = ["\n## ", "\n---\n"].map((m) => rest.indexOf(m)).filter((i) => i >= 0);
        const block = ends.length > 0 ? rest.slice(0, Math.min(...ends)) : rest;
        return block.replace(/\s+$/, "").split("\n");
    };
    const carries = CARRIED_REGIONS.map((r) => ({ ...r, lines: sliceCarry(r) }));

Three properties, each ordered by §0ah and each measured below:
  (a) a LIST of marks, not one block;
  (b) each sliced to the next level-2 heading, EXCEPT §10's, taken as the TAIL so §11 rides;
  (c) emitted under its own preamble IN DOCUMENT ORDER — §6's inside §6 (unmoved), the tail
      at the file's end after §9, with the preamble ABOVE the carried `## §10` heading so the
      next run's slice cannot swallow it and print it twice.

The bounded slice keeps BOTH end-marks ("\n## " and "\n---\n"). §6's carried bytes are
therefore unchanged by this unit — proven at §4, 47 lines before and after.

⟨grep -c '## §10' test/css-equivalence/emit-divergence-ledger.mjs⟩ → 7   (born-RED 0 → 7;
  read from the settled bytes after the last edit, not from the edit's intent)
⟨node --check test/css-equivalence/emit-divergence-ledger.mjs⟩ → SYNTAX OK, EXIT=0

NO REGENERATION OF `DIVERGENCE-LEDGER.md` IS ORDERED AND NONE WAS PERFORMED.
⟨git -C <value.js> status --porcelain -- docs/tranches/X/parse-that/DIVERGENCE-LEDGER.md⟩
  → 0 rows. The ledger stands byte-frozen (E-3); the cure is proven by emitting to scratch.

================================================================================
§3  ACCEPTANCE, MEASURED — two consecutive emissions to a scratch --out
================================================================================

⟨node test/css-equivalence/emit-divergence-ledger.mjs --out <scratch>/after-1.md \
       --pinned-value-commit 6aca8602…⟩ →
  wrote …/after-1.md — 142234 B · 1197 lines · 46 rows · empty directions 0 ·
  carried regions §6 47 lines · §10 105 lines
  EXIT=0

⟨node test/css-equivalence/emit-divergence-ledger.mjs --out <scratch>/after-2.md \
       --pinned-value-commit 6aca8602…⟩ →
  wrote …/after-2.md — 142234 B · 1197 lines · 46 rows · empty directions 0 ·
  carried regions §6 47 lines · §10 105 lines
  EXIT=0

(a) IDEMPOTENCE — the two emissions are the same bytes, not merely the same size:
  ⟨diff after-1.md after-2.md⟩ → EXIT=0, 0 lines of output
  ⟨shasum -a 256 after-1.md after-2.md⟩ →
    0d17d4bf353a0163e2dd9c7ee3870e2a2a13cee5b5a1fd168932fc5dbd2e09ba  after-1.md
    0d17d4bf353a0163e2dd9c7ee3870e2a2a13cee5b5a1fd168932fc5dbd2e09ba  after-2.md

(b) §10's RETIREMENT ROWS AND §11's HEADING SURVIVE BOTH EMISSIONS — grepped per emission,
    never inferred from one:

  | probe                                                            | after-1 | after-2 |
  |------------------------------------------------------------------|---------|---------|
  | grep -c '^## §10'                                                  |    1    |    1    |
  | grep -c '^## §11'                                                  |    1    |    1    |
  | grep -c '^#### §10\.'                                              |    4    |    4    |
  | grep -c '^#### §11\.'                                              |    3    |    3    |
  | grep -c 'RETIRED'                                                  |    4    |    4    |
  | grep -c '^#### §10.2 Rows whose premise is measured FALSE at the    |         |         |
  |   producer — RETIRED as coverage claims'                           |    1    |    1    |

  The baseline column of §1 is the same three probes at 0 / 0 / 0. The gate is the delta,
  and the delta is measured on both sides of one commit.

(c) EXACTLY ONE of each heading, in both emissions — the preamble is above the carried
    `## §10` and is not itself carried, so nothing doubles. This is the property whose
    absence grew F-e7's first double-run 47 → 286 → 525 lines.

================================================================================
§4  THE CARRY IS VERBATIM, AND IT IS A FIXED POINT
================================================================================

(a) VERBATIM — the emitted tail is the canonical bytes, not a re-rendering:
  ⟨awk '/^## §10 /{p=1} p' after-1.md > emitted-tail.txt⟩            → 105 lines
  ⟨sed -n '1084,$p' <canonical DIVERGENCE-LEDGER.md> > canon-tail.txt⟩ → 105 lines
  ⟨diff canon-tail.txt emitted-tail.txt⟩ → EXIT=0, 0 lines.
  The program authors not one byte of §10 or §11; it lifts and re-emits them.

(b) FIXED POINT — were an emission ever written to the canonical path, the NEXT run would
    carry the identical regions out of it. Checked by applying the landed slice function to
    the canonical file and to the emitted file and comparing region for region:
  ⟨node <scratch>/fixedpoint.mjs <canonical> after-1.md⟩ →
    §6:  canonical 47 lines · emitted 47 lines · IDENTICAL=true
    §10: canonical 105 lines · emitted 105 lines · IDENTICAL=true
  So idempotence does not depend on --out pointing away from the artefact — which is the
  path-dependence F-y2 measured one cure ago. Nothing was written to the canonical path.

(c) §6 UNMOVED BY THIS UNIT — 47 carried lines before the cure (§1's emission) and 47 after
    (§3's two emissions), the same region, the same end-marks. This unit adds a region; it
    does not re-cut the one F-e7 already carries.

================================================================================
§5  WHAT THIS FILE DOES NOT CLAIM
================================================================================

- It does not claim `DIVERGENCE-LEDGER.md` was regenerated. It was not, and none was
  ordered (§0ah: "No regeneration is ordered; the ledger stands (E-3)").
- It does not claim R-C5 / R-2 are discharged as ledger rows. They are discharged AS A
  MECHANISM: the residual read "a regeneration must re-append §10 and §11 or both rulings'
  rows are lost", and after this commit a regeneration re-appends them by itself. The
  ledger's own §10/§11 sentences saying "a regeneration must re-append this" remain
  standing in the ledger, immutable under E-3, now describing a cured defect.
- It does not adjudicate anything. §6, §10 and §11 are three other seats' bytes.
